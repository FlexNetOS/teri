# MiroFish → teri Parity Ledger

**Source root:** `/home/drdave/Desktop/meta/MiroFish/`
**Target repo:** `teri` (Rust)
**Harvest date:** 2026-06-13
**Status legend:** `- [ ]` not ported · `- [~]` ported, parity unproven · `- [x]` ported + parity-verified · `- [!]` blocked · `- [≠]` intentional-divergence

Dependency order: leaf units (no internal deps) first, then consumers.

---

## Layer 0 — Configuration / Bootstrap

- [x] U-001 · `backend/app/config.py:Config` · Class holding all env-var-backed config fields; `validate()` classmethod returns list of error strings for missing required vars (`LLM_API_KEY`, `ZEP_API_KEY`); fields include `ALLOWED_EXTENSIONS`, `MAX_CONTENT_LENGTH=50MB`, `OASIS_TWITTER_ACTIONS` list, `OASIS_REDDIT_ACTIONS` list, `UPLOAD_FOLDER`, `DEBUG`, optional boost-LLM vars; reads from `.env` via `python-dotenv`. Error: exits with code 1 if validate() returns non-empty list. → `teri::config::AppConfig` · deps: none · **PARTIAL — PARITY-VERIFIED 2026-06-17 (extend-Y, opus gate).** 20 value-fields ported onto teri's existing `Config` (S-002,S-004,S-006..S-022 `- [x]`): exact env-name+default+type match incl. OASIS lists (6 twitter / 13 reddit, source order, TREND+REFRESH preserved); `validate_collect() -> Vec<String>` adds the MiroFish collect-all-errors contract enforcing LLM_API_KEY **and** ZEP_API_KEY; keyless-CLI parse discipline preserved. S-001 `- [≠]` (c)-superset: teri `dotenvy::dotenv().ok()` (config.rs:112) walks parent dirs ⊇ MiroFish single-`.env`. **COMPLETE 2026-06-17 — ROLLED UP at U-002/U-003:** S-003 SECRET_KEY (`Config.secret_key`, env `SECRET_KEY`, default `"mirofish-secret-key"`, on `ApiState`) + S-005 JSON_AS_ASCII (axum `Json`/serde_json raw-UTF-8 default == `ensure_ascii=False`) are now both `- [x]` (opus PASS). With the 20 value-fields `[x]` + S-001 `[≠]`, **all U-001 symbols accounted → unit `- [x]` COMPLETE.** **OWNER-RESOLVED 2026-06-17 (was `[!]`):** the LLM_MODEL_NAME default divergence is settled — MiroFish targeted Ollama models which teri replaces with **shimmy**, so neither `gpt-4o-mini` nor `gpt-4o` is correct. teri now defaults to a shimmy-served top reasoning model: `model="OpenThinker3-7B"` (8–16GB VRAM, highest raw text/math/code reasoning — fits teri's text-only swarm-prediction workload), with `Gemma-4-12B` documented as the 16–24GB multimodal/tool-calling alternative (config.rs:63-76). `base_url` default also moved OpenAI→shimmy local `http://127.0.0.1:11435/v1`. Two new default tests (no-env → OpenThinker3-7B + shimmy base_url). This is `[!]`-class (owner architecture decision, legal), now CLOSED. (NOTE: `embed_model` default still `text-embedding-3-small` — embeddings remain GAP-OQ3-EMBED, shimmy has no `/v1/embeddings` yet; not changed here.) **Test hygiene:** porter's env-mutating tests raced on process-global env under parallel threads (observed flaky `test_debug_*`); fixed with a module-local `ENV_LOCK` mutex (`unwrap_or_else(into_inner)` poison-tolerant) guarding all 17 env-mutating tests — 5/5 parallel runs now green. 335→362 tests (+27), clippy `--all-targets -D warnings` clean.

- [x] U-002 · `backend/run.py:main` · Flask entry-point: validates config, creates app, reads `FLASK_HOST`/`FLASK_PORT` env vars (defaults `0.0.0.0`/`5001`), calls `app.run(threaded=True, debug=Config.DEBUG)`; Windows UTF-8 reconfigure at module top. → `teri::server::serve` (via `serve_cmd`) · deps: U-001, U-003 · **COMPLETE — PARITY-VERIFIED 2026-06-17 (opus PASS). S-023 `- [x]`.** `validate_collect()` first → print `配置错误:` + `  - {err}` + return Err (== `sys.exit(1)`); bind-addr faithful SUPERSET (`--addr` flag wins; else `FLASK_HOST` def "0.0.0.0" + `FLASK_PORT` def 5001 as int; 3 resolver tests); `axum::serve().with_graceful_shutdown(ctrl_c)`. Two `[≠]` (opus-accepted): Windows `sys.stdout.reconfigure` UTF-8 (inexpressible — Rust strings natively UTF-8) + Flask `debug`/`threaded` WSGI artifacts (tokio always-concurrent superset; `config.debug` still loaded). `src/main.rs`+`src/server.rs`. 17 server tests; teri 496 green.

- [~] U-003 · `backend/app/__init__.py:create_app` · Flask app factory: sets `ensure_ascii=False` on JSON encoder, calls `setup_logger()`, calls `SimulationRunner.register_cleanup()`, registers `before_request`/`after_request` middleware (request logging), registers three blueprints at `/api/graph`, `/api/simulation`, `/api/report`, adds `/health` GET. → `teri::server::create_app` · deps: U-001, U-022, U-026, U-027 · **PARTIAL — PARITY-VERIFIED 2026-06-17 (opus PASS for portable-now portion). S-025 `/health` `- [x]`; S-024 `create_app` `- [~]`.** Landed `src/server.rs::create_app(state) -> Router`: CORS permissive (== `origins:"*"`), before/after logging middleware (`请求: {method} {path}`/`响应: {status}` debug), Accept-Language→locale middleware (**rolled up U-005 S-040**), JSON raw-UTF-8 (**rolled up U-001 S-005**), `/health` → `{"status":"ok","service":"teri"}` (exact 2-key shape; the ledger's `{"status":"healthy"}` summary was WRONG — matched the actual source `__init__.py:74` `{'status':'ok','service':...}`). **Two pieces PENDING (recorded, NOT dropped → why S-024/unit stay `[~]`):** (1) 3 blueprint mounts `/api/graph|simulation|report` → **pending U-025/026/027** (marked nest placeholder, NO stub routes); (2) `register_cleanup` sim-process teardown → **pending U-023/U-049** (basic `ctrl_c` shutdown wired now). Rolls up to `- [x]` when U-025/026/027 land + cleanup wires. `src/server.rs`. 496 green.

---

## Layer 1 — Utilities (no internal service deps)

- [x] U-004 · `backend/app/utils/logger.py` · `setup_logger()` configures root logger with rotating file handler (LOG_DIR = `backend/logs/`) + console handler; `get_logger(name)` returns named logger; shortcut functions `debug/info/warning/error/critical`. Side effect: creates `logs/` directory. Error: silently falls back if dir not writable. → `teri::logging` (`src/logging.rs`) · deps: none · **PARITY-VERIFIED cycle-9 (2026-06-14).** Rotating-FILE logging (the previously-skipped capability) now ported + proven, NO `[≠]` feature-skip remains. `init_logging(level)` composes console layer + opt-in DEBUG+ file layer; size-based rotation 10MB×5 via `file-rotate` 0.8 `ContentLimit::Bytes(MAX_LOG_BYTES=10*1024*1024)` + `AppendCount::new(LOG_BACKUP_COUNT=5)` — exact match to MiroFish `RotatingFileHandler(maxBytes=10MB, backupCount=5)`; SIZE-based confirmed against file-rotate source (NOT time-based). **Rotation genuinely works (not a stub):** `test_rotation_produces_backup_after_size_limit` writes 11MB through the REAL FileRotate writer → asserts `teri.log.1` appears; `test_rotation_keeps_at_most_backup_count_files` forces ≥8 rotations → asserts backups ≤5 (ceiling proven; `AppendCount` enforces `file_number >= max_files` per suffix.rs). **Opt-in + default preserved:** `TERI_LOG_DIR` unset → console-only byte-for-byte prior behavior (no regression); set → file layer added. **Idiomatic-equivalent mappings (NOT gaps):** `get_logger(name)`→tracing `target:`; `debug/info/warning/error/critical`→`tracing::debug!/info!/warn!/error!/error!` (critical→error correct — ERROR is tracing's highest level, confirmed in tracing-core); `_ensure_utf8_stdout`→N/A (Rust stdout UTF-8 on all targeted platforms). S-026..S-035 all `- [x]`. 310→315 tests (+5 logging), clippy `--all-targets -D warnings` clean. main.rs: 2 init sites → `init_logging` (behavior-preserving).

- [x] U-005 · `backend/app/utils/locale.py` · **COMPLETE — PARITY-VERIFIED 2026-06-17 (opus PASS); 7/7 symbols `[x]`.** Landed `src/i18n/mod.rs` (+`src/i18n/locales/{zh,en,languages}.json` byte-identical embed). `t()`/`t_args()` (S-041), `get_language_instruction()` 7-locale (S-042), `with_locale`/`LOCALE` task-local replacing thread-local (S-036/S-039), `OnceLock` translations {en,zh} (S-038), `include_str!` embed (S-037). **S-040 `get_locale` NOW COMPLETE** — its request-context branch went live at U-002/U-003: `server::accept_language_middleware` reads Accept-Language, validates via `i18n::is_supported_locale` else "zh", scopes the handler in `with_locale` (4 tests). ROLLED UP U-012's S-163/S-164 (task messages route through `i18n::t()`). → `teri::i18n` · deps: none

- [x] U-006 · `backend/app/utils/retry.py` · exponential backoff; re-raises on final attempt; wraps specified exception types only. → teri per-adapter inline retry (all 3 adapters) · deps: none · **PARITY-VERIFIED cycle-4 (RESOLVED).** max_delay clamp (MAX_BACKOFF_SECS=30) == retry.py:59; jitter omission `- [≠]` (stochastic, non-contractual); retry-fires/cap/no-spurious tested. **Recovery path now PROVEN:** `test_openai_retry_recovers_after_503` (503-then-200 via module-static `AtomicUsize` + `.matches(|_req| C.fetch_add(1,SeqCst)==0)`; ~6s suite time confirms real 2^1 backoff). 242 green. S-043/S-044 → `- [x]`.

- [x] U-007 · `backend/app/utils/zep_paging.py` · `_fetch_page_with_retry(client, graph_id, cursor, page_size, entity_type, max_retries)` with exponential backoff (default `_DEFAULT_PAGE_SIZE=100`, `_MAX_NODES=2000`, `_DEFAULT_MAX_RETRIES=3`); `fetch_all_nodes(client, graph_id)` paginates via UUID cursor until no more pages (stops at `_MAX_NODES`); `fetch_all_edges(client, graph_id)` same pattern for edges. Error: logs warning on retry failure; raises on final failure. → `teri::integrations::zep::paging` · deps: none
  - **✅ U-007 COMPLETE — VERIFIED 2026-06-17 (opus parity PASS, DECISION-12, map-onto-substrate, NO new code):** the whole module is Zep-Cloud network pagination; teri's in-process petgraph has no network/cursor/pages/transient-I/O. S-054 `fetch_all_nodes` / S-055 `fetch_all_edges` `[x]` map-onto — subsumed by U-016's parity-verified `KnowledgeGraphEntityReader::get_all_nodes`/`get_all_edges` over `KnowledgeGraph::get_all_entities`(`node_weights().collect()`)/`get_all_edges`(`edge_references()`) which return EVERY node/edge (no limit/filter/pagination — "all" means all, deterministic insertion order). S-053 `_fetch_page_with_retry` + S-049/S-051/S-052 (page_size/retries/delay) `[≠]` inexpressible (retry only remote-SaaS transient errors; in-memory read has no I/O to retry — consistent w/ U-016 S-216). **S-050 `_MAX_NODES=2000` cap `[≠]` strict-SUPERSET** — the gate adversarially checked EVERY consumer (MiroFish graph_builder/zep_tools/zep_entity_reader + teri AgentPool/filter_defined_entities/prepare_simulation/`summarize_entities`) for a ≤2000 dependency and found NONE; the strongest candidate (LLM context) is bounded independently by `ENTITIES_PER_TYPE_DISPLAY=20`/`ENTITY_SUMMARY_LENGTH=300`/`MAX_CONTEXT_LENGTH=50000`, never by the cap. teri returns the full in-memory set = more faithful, not a disguised skip. **Unblocks U-017, U-021.** Verdict in `findings/parity.md`.

- [x] U-008 · `backend/app/utils/llm_client.py:LLMClient` · Wraps OpenAI-compatible API (points at Alibaba DashScope qwen-plus by default via `LLM_BASE_URL`/`LLM_MODEL_NAME`); `chat(messages, temperature, max_tokens, response_format)` calls `client.chat.completions.create`, strips `<think>...</think>` from response; `chat_json(messages, temperature, max_tokens)` calls `chat` with `{"type":"json_object"}`, strips markdown code block fences, parses JSON. Error: raises `ValueError` if `LLM_API_KEY` unset; raises `ValueError` if JSON parse fails with raw response. Side effect: none (stateless HTTP calls). → `teri::integrations::llm::LlmClient` · deps: U-001 · **PARITY-VERIFIED (cycle-4): GAP-6 RESOLVED.** `strip_think` (llm.rs:21) == `re.sub(r'<think>[\s\S]*?</think>','').strip()` (llm_client.py:67); `strip_json_fence` (llm.rs:49) == llm_client.py:94-97 fence strip. Both applied in complete/complete_json across ALL 3 adapters (OpenAI/Anthropic/Gemini); parse-fail→TeriError::Llm ≈ ValueError. 17 differential tests green (strip unit + per-adapter HTTP). teri LLM adapter = PROVEN SUPERSET of MiroFish chat/chat_json (adds Anthropic + Gemini + true SSE streaming). All 4 symbols S-056..S-059 `- [x]`.

- [x] U-009 · `backend/app/utils/file_parser.py` · `_read_text_with_fallback(path)` multi-level encoding detection (chardet → charset-normalizer → latin-1); `FileParser.is_supported(filename)` checks against `Config.ALLOWED_EXTENSIONS`; `FileParser.extract_text(file_path)` dispatches to `_extract_from_pdf` (PyMuPDF), `_extract_from_md`, `_extract_from_txt`; `FileParser.extract_from_multiple(paths)` returns concatenated texts; module-level `split_text_into_chunks(text, chunk_size, overlap)` splits by character count with overlap. Error: raises on unsupported format; silently skips failed PDF pages. → `teri::seed` (`SeedIngestor` at `src/seed/mod.rs`) · deps: U-001 · **PARITY-VERIFIED cycle-6 (2026-06-14).** Differential vs MiroFish `file_parser.py`+`config.py`. S-060/S-061/S-064/S-065/S-066/S-067/S-068 → `- [x]`; S-062/S-063 → `- [≠]` (json superset + permissive `from_file` policy — both no-downgrade: teri's supported set is a SUPERSET of MiroFish's, `is_supported` is the caller-side gate). S-069 distributed to U-013 (already `- [x]`). **Encoding fallback proven via direct `encoding_rs` run:** UTF-8 fast path; GBK `[D6 D0 CE C4]`→"中文" `had_errors=false`; Latin-1 `café` bytes→GBK `had_errors=true` so guard (mod.rs:167-170) REJECTS → Windows-1252 → "café" (NO Latin-1 mis-decode-as-GBK); Windows-1252 backstop never errors (no `from_utf8_lossy` mojibake). `from_files` concat byte-exact + per-file error tolerance. 263→275 (+12) tests green; clippy `--all-targets -D warnings` clean.

- [x] U-010 · `backend/scripts/action_logger.py` · `PlatformActionLogger(platform, base_dir)` writes per-platform `{base_dir}/{platform}/actions.jsonl`; `SimulationLogManager(sim_dir)` aggregates twitter+reddit loggers + main `simulation.log`; `ActionLogger` (legacy compat) + module `get_logger()`. JSONL: one JSON object per line, UTF-8, `ensure_ascii=False`. → `teri::sim::action_logger` (`src/sim/action_logger.rs`) · deps: none · **COMPLETE — PARITY-VERIFIED 2026-06-17 (port-fresh, opus gate). 28/28 symbols `- [x]` (S-070..S-097).** Faithful: every per-event JSON key-set exact (log_action 8 keys, round_start/end 4, sim_start/end 5; legacy ActionLogger entries carry `platform`); `action_args` None→`{}`, `result` None→null, `success` default true; `total_rounds = time_config.total_simulation_hours(def 72)*2`, `agents_count = len(agent_configs)`; append-mode JSONL accumulates; ensure_ascii=False (中文 raw); chrono::Local naive isoformat + µs-omission (shared `python_isoformat_local`, made `pub(crate)` — project tests unregressed). SimulationLogManager main `simulation.log` ported as a direct dual-sink writer (NOT routed through global tracing): file `"%Y-%m-%d %H:%M:%S - LEVEL - msg"` (mode='w' truncate), stderr `"[%H:%M:%S] msg"`; INFO level → **debug suppressed** from both sinks (faithful to `setLevel(INFO)`); lazy twitter/reddit return same instance; global singleton `OnceLock<Mutex<Option<ActionLogger>>>` (reset-on-path + `actions.jsonl` default). **Gate caught + fixed 2 real issues:** (1) S-083 `log()` level dispatch narrowed Python's `getattr` method-name resolution (`critical/fatal/warn/exception`→INFO) → fixed with full alias map + new `LogLevel::Critical`. (2) S-096/S-097 global-singleton tests raced under parallelism → fixed with the U-001 `ENV_LOCK`-style serialization mutex + per-test reset (33/33 stable ×3). **One `[≠]` (opus-ACCEPTED):** `logger.exception()` trailing `sys.exc_info()` traceback line — genuinely-inexpressible Python ambient-exception state, ERROR-level dispatch ported, zero source call sites. 33 module tests; teri 479 green, clippy `--all-targets` clean.

---

## Layer 2 — Models (FS persistence, no network)

- [x] U-011 · `backend/app/models/project.py:ProjectManager` · Class-method-based filesystem persistence; `PROJECTS_DIR = uploads/projects`; `ProjectStatus` enum (CREATED, ONTOLOGY_GENERATED, GRAPH_BUILDING, GRAPH_COMPLETED, FAILED); `Project` dataclass with `to_dict()`/`from_dict()` (JSON round-trip). Methods: create/save/get/list/delete project; save_file_to_project; save/get_extracted_text; get_project_files. → `teri::models::project` (`src/models/project.rs`) · deps: none · **COMPLETE — PARITY-VERIFIED 2026-06-17 (port-fresh, opus gate). 40/40 symbols `- [x]` (S-098..S-137).** Faithful: ProjectStatus snake_case serde == Python `.value`; Project 15-key to_dict (ensure_ascii=False → non-ASCII raw); from_dict `.get(k,default)` tolerance incl. project_id REQUIRED→Err; PROJECTS_DIR from config.upload_folder; create_project `proj_`+12-hex; get_project missing→None / corrupt→Err (ledger summary "None on corrupt" was WRONG — matched the CODE); delete_project absent→false (ledger "raises" was WRONG — matched CODE); save_file_to_project FileStorage→`&[u8]`, 8-hex+lc-ext, 4-key return; list_projects created_at-desc+limit; chrono::Local naive ISO (matches `datetime.now()`, distinct from task.py `utcnow`) + µs-omission. **Gate caught + fixed 2 real downgrades:** (1) create_project returned a CLONE → stale `updated_at` (≠ persisted); fixed to same-object mutation (returned `updated_at`==persisted, ≥created_at). (2) `from_dict` strict `Vec<ProjectFile>` parse silently collapsed a legacy/malformed `files` list to `[]`; fixed to `Vec<serde_json::Value>` verbatim passthrough (== Python untyped `List[Dict]`). One `[≠]`: on-disk JSON key-order (insertion vs alphabetical) — non-contractual, accepted. 23 module tests; teri 446 green, clippy `--all-targets` clean.

- [x] U-012 · `backend/app/models/task.py:TaskManager` · Singleton (double-checked locking pattern with `_instance`); in-memory task registry protected by `threading.Lock`; `TaskStatus` enum (PENDING, PROCESSING, COMPLETED, FAILED); `Task` dataclass with `to_dict()`; Methods: `create_task(task_type, metadata)` → uuid task_id; `get_task(id)` → `Optional[Task]`; `update_task(id, status, progress, message)`; `complete_task(id, result)`; `fail_task(id, error)`; `list_tasks(task_type)` → list; `cleanup_old_tasks(max_age_hours)` removes completed/failed tasks older than threshold. Contract: thread-safe; singleton across threads. → `teri::task` (`src/task.rs`) · deps: none · **COMPLETE — PARITY-VERIFIED 2026-06-17 (port-fresh, opus gate). 29/29 symbols `- [x]`.** All fields/methods/branches ported: TaskStatus serde lowercase = Python `.value`; Task 11 fields + `to_dict`; singleton idiom-mapped `OnceLock`+`parking_lot::Mutex` (one shared registry, pointer-eq + cross-handle tests prove it — observable contract preserved, not a downgrade); create/get/update(partial)/complete/fail/list(desc created_at sort)/cleanup_old_tasks (`<` cutoff, only COMPLETED/FAILED, never PENDING/PROCESSING). Gate caught + fixed S-155 `to_dict` timestamp bug — Python `datetime.isoformat()` omits the µs fraction when µs==0; teri's `%.6f` always emitted `.000000` → fixed via `python_isoformat()`. **ROLLED UP 2026-06-17 (U-005 landed):** S-163/S-164 (`complete_task`/`fail_task` `message`) now route through `crate::i18n::t("progress.taskComplete")`/`t("progress.taskFailed")` (locale-parameterized, exactly as MiroFish task.py:153/162) → both `- [x]`, unit COMPLETE. zh-default still yields "任务完成"/"任务失败" when LOCALE unset (existing tests green). Unblocks S-189 `build_graph_async`→U-012 (next). teri 423 green, clippy `--all-targets` clean.

---

## Layer 3 — External Integration Services

- [x] U-013 · `backend/app/services/text_processor.py:TextProcessor` · Static class; `extract_from_files(file_paths)` delegates to `FileParser`; `split_text(text, chunk_size=500, chunk_overlap=50)` splits text by character count with overlap for Zep episode batching; `preprocess_text(text)` normalizes whitespace; `get_text_stats(text)` returns dict with char/word/line counts. → `teri::seed::text_processor` · deps: U-009 · **PARITY-VERIFIED cycle-5 (2026-06-14).** Differential vs MiroFish source (`file_parser.py:161-202` split + `text_processor.py:37-70` preprocess/stats), 26 cases, ALL match: split (13 cases — empty/blank/short/exact/remainder/overlap/no-sep/period-space/chinese-fullstop/multibyte/hiragana/sep-below-30%/mixed-seps), preprocess (9), stats (4). split_text boundary-backtrack + 30%-threshold + overlap proven equivalent; UTF-8-safe (char-boundary windowing, no mid-char split); stats are Unicode-scalar/whitespace-split/`\n`-count+1. S-167/S-169/S-170/S-171 `- [x]`; S-168 `- [≠]` (FileParser delegation, no Rust equiv). 263 tests green. Comment-only nit: doc lines 152-154 mis-says "preserve leading whitespace" — code (`l.trim()`) correctly strips both ends, parity-proven.

- [x] U-014 · `backend/app/services/ontology_generator.py:OntologyGenerator` · `generate(...)` calls LLM with `ONTOLOGY_SYSTEM_PROMPT` → `{entity_types,edge_types,analysis_summary}`; `_validate_and_process` enforces 10 entity types (8 specific + Person/Organization fallbacks), max 10 edge types, PascalCase entities, SCREAMING_SNAKE edges, reserved-attr avoidance; `_to_pascal_case`; `generate_python_code`. → `teri::services::ontology` (`src/services/ontology.rs`) · deps: U-008✅, U-005✅ · **COMPLETE — PARITY-VERIFIED 2026-06-17 (opus PASS, live differential vs Python). 9/9 symbols (8 `[x]` + S-180 `[≠]`).** S-172 prompt byte-verbatim (2759 chars); S-173 `to_pascal_case` (Python `.capitalize` semantics — `ABC`→`Abc`; `Unknown` default; 13-case differential identical); S-176 `generate` faithfully passes `[system,user]` + temperature 0.3 + max_tokens 4096 via the DECISION-7 chat surface; S-178 `build_user_message` CHAR-based 50000 truncation (`.chars()`, not bytes — proven with Chinese) + verbatim notice/footer; S-179 `_validate_and_process` ALL branches differential-identical (PascalCase rename + entity_name_map source_target remap, edge upper-case, dedup-keep-first, char-based 100-char desc truncation, exact Person/Organization fallback dicts, end-removal when current+needed>10, 10/10 caps). **S-180 `generate_python_code` `[≠]` (opus-accepted):** ZERO callers in MiroFish (dead code) + emits Zep-Cloud Python class strings for a SaaS teri replaced with native petgraph; real path is `set_ontology` from the validated DICT (ported); maps onto `EntityKind::Custom` via future S-192. **Built on U-008-extend (DECISION-7, extend-Y on U-008):** added `chat`/`chat_json(&[ChatMessage],&ChatOptions)` to `LlmClient` (+ ChatRole/ChatMessage/ChatOptions) on all 3 adapters — additive superset, the faithful full-signature port of S-058/S-059 (messages+temp+max_tokens); opus-confirmed NO regression (complete*/stream byte-identical, 0 deletions in llm.rs diff; production adapters fully implemented, only `#[cfg(test)]` mocks stubbed). Reused next by U-019/U-024/U-018. 49 new tests; teri 545 green, clippy `--all-targets` clean.

- [x] U-015 · `backend/app/services/graph_builder.py:GraphBuilderService` · Builds Zep Standalone Graph; `build_graph_async(text, ontology, graph_name, chunk_size=500, chunk_overlap=50, batch_size=3)` → task_id (background thread, captures locale before spawn); `_build_graph_worker(task_id, ...)` executes: create_graph → set_ontology → split_text → add_text_batches → _wait_for_episodes → _get_graph_info → complete_task; `create_graph(name)` → graph_id (`mirofish_{uuid16}`); `set_ontology(graph_id, ontology)` dynamically creates Pydantic EntityModel/EdgeModel subclasses, calls `client.graph.set_ontology`; `add_text_batches(graph_id, chunks, batch_size, progress_callback)` → list of episode UUIDs (sleeps 1s between batches); `_wait_for_episodes(episode_uuids, progress_callback, timeout=600)` polls each episode for `processed=True` every 3s, breaks on timeout; `get_graph_data(graph_id)` → nodes+edges dict with temporal fields; `delete_graph(graph_id)`. Runtime contract: background daemon thread (dies with process); progress reported via task manager; locale propagated via set_locale() before spawn. Error: `_build_graph_worker` catches all exceptions → `fail_task`. → `teri::services::graph_builder` · deps: U-007, U-012, U-013, U-005 · **PARITY-VERIFIED 2026-06-14 (S-190):** `KnowledgeGraph::build()` 2-pass LLM extraction (entity→relation); all 5 branches (happy/empty/dup/unknown-ref/llm-error) match MiroFish resilience contract; S-190 `- [x]`; Zep mechanics `- [≠]`; 156-test baseline intact. **GAP-U015-1 RESOLVED cycle-5 (2026-06-14):** chunking via parity-proven `text_processor::split_text`; entity extraction per chunk → name-deduped merge; relation extraction per chunk over full deduped entity set; small docs → 1 chunk = identical single-pass path; blank-doc → empty graph (tolerant). 263 tests green. **S-189 + S-192 PARITY-VERIFIED 2026-06-17 (opus PASS/PASS — U-015 COMPLETE):** `build_graph_async` (`src/services/graph_builder.rs:84`) — spawn contract (task_id immediate + in registry), worker lifecycle Ok→complete_task COMPLETED / Err→fail_task FAILED w/ error string (spawn target calls same `build_graph_worker_inner` the tests drive → coverage refactored not lost), milestones 5/15/20/20-60/90/100% ported, result = teri-native superset of MiroFish graph_id (embeds serialized graph). `KnowledgeGraph::set_ontology` (`src/graph/mod.rs:238`) NOT inert — recorded entity+edge type names verified to reach extraction prompts AND parsers for both entities and edges; differential test emits `Custom("MediaOutlet")`+`Custom("COVERS_TOPIC")`; built-in names still map to built-ins. `EntityKind::Custom(String)`+`RelationKind::Custom(String)` additive; serde wire form of existing variants unchanged (round-trip verified — bare strings; existing graphs round-trip identically); no exhaustive match outside graph/mod.rs (Display-only consumers). `build<L>` signature byte-identical; 6 build tests unchanged. `[≠]` items (create_graph 10%, wait_for_episodes 60-90%, batch_size, Zep-SDK Pydantic synthesis / RESERVED_NAMES / Field(default=None), S-181..S-188/S-191/S-193-S-197) ALL CHALLENGED and survive: Zep-server/SDK-inexpressible or non-contractual, no observable teri output dropped — none a disguised portable-feature skip. 550 lib tests green; clippy --all-targets -D warnings clean. **U-015 `- [x]` — rollup satisfied (all 19 symbols `- [x]`/`- [≠]`).**

- [x] GAP-1 / OQ-2 · `Relation.valid_at` temporal field · **PARITY-VERIFIED 2026-06-14 (map-onto-substrate).** MiroFish/Zep edges carry temporal validity windows (`valid_at`/`invalid_at`) for `panorama_search` active-vs-historical classification. Added to `src/graph/mod.rs`: field `pub valid_at: Option<(u64, Option<u64>)>` with `#[serde(default)]` (backward-compat); `Relation::with_validity()` constructor; `is_active_at(t) -> bool` helper; `KnowledgeGraph::partition_edges_at(t)` splitting active/historical; `parse_valid_at_from_json()` parses optional validity from LLM JSON (array or object form); `EdgeTriple` type alias for clippy. 3 struct-literal sites updated to `valid_at: None`. **Verified:** `is_active_at` half-open `[start,end)` semantics reproduce Zep's `is_expired`/`is_invalid` active-vs-historical contract (`panorama_search` zep_tools.py:1185-1206); serde backward-compat PROVEN (old JSON `{"kind":"RelatedTo","weight":0.5}` → `valid_at=None`, no-downgrade hold); 6 GAP-1 symbols all `- [x]`. **ENABLES** U-017 `panorama_search` (still `- [ ]`) — the temporal substrate it needs now exists.

- [x] GAP-2 / OQ-3 · `query_vec_similarity` cosine search · **PARITY-VERIFIED 2026-06-14 (map-onto-substrate).** De-stubbed in `src/memory/mod.rs`. Real cosine similarity search over stored `agent:{uuid}:vec:` entries via spawn_blocking redb scan. Dimension-mismatch skipped gracefully; zero-norm skipped; empty store returns Ok([]); top_k >= available returns all sorted. **Verified:** cosine math is GENUINE (magnitude-normalized via `dot/(|q|·|e|)`, NOT raw dot — confirmed by independent differential: a high-magnitude wrong-direction vector correctly loses to a directionally-aligned one); all 6 branches tested. Reproduces the SEARCH half of Zep `insight_forge`/`quick_search` semantic ranked-results contract (`search_graph` zep_tools.py:464 → ranked, `limit`-capped facts). Test count: 171 (was 156); 156 baseline intact. **ENABLES** U-017 `insight_forge`/`quick_search`, U-021/U-024 retrieval consumers (still `- [ ]`) — the ranked-search substrate they need now exists.

- [x] GAP-OQ3-EMBED · Embedding generation substrate · **RESOLVED 2026-06-17 — owner chose (a) AND it's wired end-to-end.** The search half (`query_vec_similarity`, GAP-2) was already done. The generation half (text → vector) now has a real backend: **(a) shimmy gained a real `POST /v1/embeddings`** — candle-transformers BERT sentence-transformer, default `all-MiniLM-L6-v2` 384-dim, mean-pool + L2-normalize, NO fake/random (FlexNetOS/shimmy#6, MERGED). **AND (b) teri got `EmbeddingClient`** (`src/embedding.rs`): OpenAI-compatible HTTP client mirroring `OpenAiAdapter`, POSTs `{model,input:string|[string]}` to `{base_url}/embeddings` (base_url=shimmy `http://127.0.0.1:11435/v1`), parses `data[].embedding` with defensive index-sort for batch order, keyless-safe (no auth header when api_key empty), errors→TeriError::Llm/Http. `embed(&str)->Vec<f32>` + `embed_batch(&[String])->Vec<Vec<f32>>`. embed_model default changed `text-embedding-3-small`→`all-MiniLM-L6-v2` (shimmy's served model). Behaviorally equivalent to MiroFish's OpenAI-embeddings generation (same API contract, ollama/OpenAI→shimmy substrate swap per owner). 7 httpmock tests; teri 371 green. This UNBLOCKS the embeddings-backed semantic-search path in U-017 (insight_forge/quick_search) and U-021/U-024 retrieval consumers (still `- [ ]` — they now have both halves of vec-similarity available).

- [x] GAP-ACTION-TAXONOMY · MiroFish/OASIS social-media action taxonomy in `Action` enum · **RESOLVED — PARITY-VERIFIED 2026-06-14 (cycle-3 RE-VERIFY): both cycle-3 defects fixed & differentially confirmed; the social Action taxonomy is now READY for U-022/U-028/U-029/U-030 (which stay `- [ ]` — they need the full sim).** PORTED 2026-06-14 (cycle 3); FIX PASS applied 2026-06-14 (cycle-3 FIX). `SocialAction` sub-enum in `src/sim/mod.rs`; `Action::Social(SocialAction)` variant (5 generic variants intact). Taxonomy: 13 variants (CREATE_POST, LIKE_POST/LIKE_COMMENT, DISLIKE_POST/DISLIKE_COMMENT, REPOST, QUOTE_POST, FOLLOW, CREATE_COMMENT, SEARCH_POSTS, SEARCH_USER, MUTE, TREND, DO_NOTHING). **FIX-1 (TREND):** `SocialAction::Trend` added — no-arg variant; parser arm "TREND"/"trend"→Trend; Display "Performed trend operation"; importance 0.25 (browse/discovery band); apply routes generic social event path. **FIX-2 (Like/Dislike discriminant):** `TargetKind {Post,Comment}` enum added to `src/sim/mod.rs`; `Like{target_kind,target_id}` and `Dislike{target_kind,target_id}` carry the discriminant; LIKE_POST→Post, LIKE_COMMENT→Comment, DISLIKE_POST→Post, DISLIKE_COMMENT→Comment; Display "Liked post: X" vs "Liked comment: X" (matching to_episode_text :70-81 vs :153-164 distinct render paths); memory content likewise distinct. REFRESH omission CORRECT (`- [≠]` S-TAX-020). Build: clean. Clippy `--all-targets -D warnings`: clean. Tests: **220 passed** (was 209; +11 new: 4 Trend + 5 Like/Dislike post-vs-comment distinct + 2 display variants). GAP-ACTION-TAXONOMY pending re-verify by parity-verifier.

- [!] GAP-SOCIAL-WORLDSTATE · Rich social world-state application deferred · `WorldState::apply`/`apply_at` accepts `Action::Social(...)` and records the event generically (same path as generic actions). Rich social-world-state application — timeline, post store, engagement counts (likes/dislikes/reposts), follower graph, comment threads — is the dedicated work of the social-sim reimplementation units: U-022 (SimulationRunner), U-028 (twitter runner), U-029 (reddit runner), U-030 (parallel runner). These units stay `- [ ]`. Not a downgrade of this cycle's work: the Action taxonomy unit (GAP-ACTION-TAXONOMY `- [~]`) is complete; the social-world-state model is a separate unit boundary. Enables U-022/028/029/030 to consume the typed `SocialAction` variants directly once ported.

- [x] U-016 · `backend/app/services/zep_entity_reader.py:ZepEntityReader` · `EntityNode` dataclass with `related_edges`, `related_nodes`, `get_entity_type()`; `FilteredEntities` dataclass; `get_all_nodes(graph_id)` paged via `fetch_all_nodes`; `get_all_edges(graph_id)` paged; `get_node_edges(node_uuid)` uses `client.graph.node.get_entity_edges` with retry; `filter_defined_entities(graph_id, defined_entity_types, enrich_with_edges)` → `FilteredEntities` filtering nodes whose labels include custom types beyond Entity/Node; `get_entity_with_context(graph_id, entity_uuid)` → `Optional[EntityNode]` with retry; `get_entities_by_type(graph_id, entity_type, enrich_with_edges)` → list. Error: returns None / empty list on Zep failures. → `teri::services::entity_reader` · deps: U-007 · **EntityNode/FilteredEntities DTOs PARITY-VERIFIED 2026-06-17 (opus PASS, differential vs Python).** S-198..S-213 (16 symbols) all `[x]` in `teri::services::entity_reader` (`src/services/entity_reader.rs`). **ZepEntityReader machinery (S-214..S-222) PORTED 2026-06-17 as `KnowledgeGraphEntityReader<'a>` in the same module.** `[≠]` fields: `summary=""` (DECISION-9 Q2, inexpressible: no per-entity summary in teri; consumer U-018 falls back to "A {type} named {name}"), `attributes={}` (DECISION-9 Q2, no attr bag; consumer guards `if entity.attributes:`), edge `fact=""` (DECISION-9 Q4, Zep LLM-generated sentence; consumer uses edge_name+direction fallback), edge `uuid=""` (DECISION-9 Q4, no consumer reads it), edge `attributes={}` (DECISION-9 Q4), `_call_with_retry` (DECISION-9 Q9 [≠] non-contractual: in-process petgraph has no I/O), `graph_id` param (DECISION-9 Q1 [≠] Zep server handle: bound &KnowledgeGraph is selector), Zep api_key/client construction (DECISION-9 Q1 [≠] Zep-auth inexpressible). **PORTED contracts**: except→None (get_entity_with_context, invalid/missing uuid), except→[] (get_node_edges, invalid/missing uuid), Entity/Node-skip filter (verbatim — always-pass in teri, typed entities), enrich via get_neighbor_relations (O(degree), provably same set as O(n·e) scan), total_count/filtered_count semantics, entity_types HashSet, related_edges/nodes exact shape. **Additive**: KnowledgeGraph::get_entity_by_id(Uuid)->Option<&Entity> added (reads existing index_by_id, zero blast radius). 39 new tests, 712 total (were 673) — build+clippy --all-targets -D warnings clean. **UNIT PARITY-VERIFIED 2026-06-17 (opus PASS — self-loop FAIL→fix→PASS re-verified).** The cycle-prior FAIL (self-loop edge double-counted in get_node_edges/filter_defined_entities/get_entity_with_context: teri 2 vs MiroFish 1) is FIXED: guard `edge.source()==*idx` in the Incoming pass of `get_neighbor_relations` (graph/mod.rs:348) drops a self-loop from the incoming pass so it is emitted ONCE as outgoing, matching MiroFish's exclusive if/elif (zep_entity_reader.py:288-303). Re-verified across all 3 reader paths (len=1, direction=outgoing, target=self). 4-case trace (self-loop + normal-outgoing + normal-incoming + bidirectional) PROVED the guard fires ONLY for self-loops — normal incoming (source≠idx) and bidirectional both unchanged/preserved. **U-018 NO REGRESSION confirmed**: U-018 `build_entity_context` (agent/mod.rs:1253) shares `get_neighbor_relations`; MiroFish `_build_entity_context` (oasis_profile_generator.py:434-453) iterates `related_edges` which is itself produced by the SAME exclusive if/elif (filter_defined_entities L288-303) → MiroFish U-018 also sees a self-loop ONCE. The fix brings teri U-018 to parity (was double-counting against U-018's contract too); never regresses it. All 104 agent tests + 40 reader tests + 46 graph tests pass; full suite 713 passed/0 failed; clippy --all-targets -D warnings clean. ALL S-198..S-222 now `[x]`/`[≠]` (S-215/S-216 are challenge-surviving `[≠]`: Zep-auth client + retry are genuinely inexpressible/non-contractual for an in-process petgraph read — no distinct observable output dropped; the except→None/[] contracts ARE ported). **UNIT U-016 DONE.**

- [ ] U-017 · `backend/app/services/zep_tools.py:ZepToolsService` · High-level Zep retrieval tools for ReportAgent; `SearchResult`, `NodeInfo`, `EdgeInfo`, `InsightForgeResult`, `PanoramaResult`, `AgentInterview`, `InterviewResult` dataclasses; Methods: `search_graph(graph_id, query, limit=10, scope="edges")` → `SearchResult` (Zep hybrid search + cross-encoder reranker, falls back to `_local_search` on failure); `_local_search` keyword-scored fallback; `get_all_nodes/edges`; `get_node_detail`; `get_node_edges`; `get_entities_by_type`; `get_entity_summary`; `get_graph_statistics`; `get_simulation_context`; `insight_forge(graph_id, query, sim_req, context, max_sub_queries=5)` → LLM-generates sub-queries, runs semantic search per sub-query, enriches entity/relationship data, returns `InsightForgeResult`; `panorama_search(graph_id, query, include_expired=True, limit=50)` → all nodes+edges classified active vs historical by temporal fields; `quick_search(graph_id, query, limit=10)` → `SearchResult`; `interview_agents(simulation_id, requirement, sim_req, max_agents=5, custom_questions)` → loads agent profiles (reddit JSON or twitter CSV), LLM selects agents, generates questions, calls `SimulationRunner.interview_agents_batch(..., platform=None)` for dual-platform interview, parses results. Runtime contract: LLM calls lazy-initialized; MAX_RETRIES=3, RETRY_DELAY=2.0s exponential backoff. Error: falls back to local search on Zep API failure; returns partial result on interview API failure. → `teri::services::zep_tools` · deps: U-007, U-008, U-005

- [x] U-018 · `backend/app/services/oasis_profile_generator.py:OasisProfileGenerator` · `OasisAgentProfile` dataclass with `to_reddit_format()` (JSON file) and `to_twitter_format()` (CSV row); `generate_profile_from_entity(entity, graph_id)` → `OasisAgentProfile`: determines individual vs group via `_is_individual_entity`/`_is_group_entity`, enriches entity context via Zep search, calls LLM with persona prompt, falls back to `_generate_profile_rule_based`; `generate_profiles_from_entities(entities, graph_id, progress_callback)` sequential generation; `save_profiles_realtime(profiles, sim_dir)` writes JSON after each profile; `generate_single_profile(...)` single-profile wrapper; `save_profiles(profiles, sim_dir)` writes final `reddit_profiles.json` + `twitter_profiles.csv`; `_save_reddit_json`; `_save_twitter_csv`. Contract: profile generation is sequential (no parallelism in base class); `parallel_profile_count` is handled in caller `SimulationManager.prepare_simulation`. Error: falls back to rule-based profile on LLM failure; JSON parse retry with `_fix_truncated_json`. → `teri::services::oasis_profile_generator` · deps: U-008, U-016, U-005

- [x] U-019 · `backend/app/services/simulation_config_generator.py:SimulationConfigGenerator` · Generates OASIS simulation config JSON from entity profiles; `AgentActivityConfig`, `TimeSimulationConfig`, `EventConfig`, `PlatformConfig`, `SimulationParameters` dataclasses; `generate_config(entities, sim_req, graph_id, profiles, sim_dir)` → saves `simulation_config.json`; stages: `_build_context` (entity summaries), `_generate_time_config` (LLM → parse), `_generate_event_config` (LLM → parse), `_assign_initial_post_agents` (LLM assigns which agents start with initial posts), `_generate_agent_configs_batch` (LLM generates per-agent twitter/reddit config); `_call_llm_with_retry` with up to 3 retries; `_fix_truncated_json`; `_generate_agent_config_by_rule` fallback. Includes `CHINA_TIMEZONE_CONFIG` constant. → `teri::services::simulation_config_generator` · deps: U-008, U-005 · **SUB-CYCLE (a) of 4 — DATA MODEL VERIFIED 2026-06-17 (opus PASS, byte-exact differential vs standalone Python).** S-374..S-429 (56 symbols) all `[x]`: `china_timezone_config()` + 5 dataclasses (`AgentActivityConfig`/`TimeSimulationConfig`/`EventConfig`/`PlatformConfig`/`SimulationParameters`) with every default byte-identical to Python, `to_dict` (13 keys, declaration order, nested asdict-recursive) + `to_json` (2-space, ensure_ascii=False). Three full `to_json()`/config scenarios diff-IDENTICAL to `json.dumps(asdict/to_dict, ensure_ascii=False, indent=2)`. preserve_order Cargo change is a crate-wide PARITY GAIN (no MiroFish `sort_keys=True`; Flask 3.1.2 → insertion order) — no verified-unit regression; U-011 latent key-order `[≠]` is now actually-ordered. **Sub-cycles (b) SimulationConfigGenerator class, (c) LLM/generation stages, (d) fallback/json-fix REMAIN `- [ ]` (S-430+).** UNIT NOT done. · **SUB-CYCLE (b) of 4 — LLM FOUNDATION + TIME/EVENT STAGES PARITY-VERIFIED 2026-06-17 (opus PASS, differential vs Python).** S-430..S-449 excl S-439 (19 symbols) all `[x]` in `teri::services::simulation_config`: 7 class constants byte-exact (50000/15/10000/8000/300/300/20); `_build_context`/`_summarize_entities` CHAR-based truncation (proven w/ CJK); `_generate_time_config`/`_generate_event_config` prompts diffed **byte-for-byte** vs source (time 1207==1207, event 562==562, zero diff) incl. `get_language_instruction()` placement + English PascalCase tail; `max_agents_allowed=max(1,int(n*0.9))`; `_get_default_time_config`/`_parse_time_config` (all clamp branches) + `_parse_event_config` (`scheduled_events=[]` confirmed hardcoded in SOURCE L723, not a shortcut); `_fix_truncated_json` (brace/bracket balance + trailing-quote, order-exact) + `_try_fix_config_json` (both regexes). **`_call_llm_with_retry` (S-442):** 3 attempts, temp 0.7/0.6/0.5 via ChatOptions, max_tokens None, raw `chat` (NOT chat_json). **finish_reason mapping (strategy a, opus-accepted):** teri's chat can't surface finish_reason (DECISION-7, inexpressible) → always-salvage-on-parse-fail subsumes Python's length-branch; adversarially probed 8 inputs — identical on all contractual JSON-object inputs; one non-contractual whitespace-collapse edge (unbalanced-JSON + non-length finish_reason, unreachable under json_object contract) where Rust mangles LESS — NO salvage capability lost. See parity.md verdict block. **Sub-cycles (c) S-450/S-451 + (d) S-452 + generate_config orchestration S-439 REMAIN `- [ ]`.** UNIT NOT done. · **SUB-CYCLE (c) of 4 — agent-config generation — FAIL 2026-06-17 (opus, 2/3).** S-451 `_generate_agent_configs_batch` → `[x]` (prompt byte-IDENTICAL 1657B incl embedded json.dumps ensure_ascii=False indent=2; system_prompt byte-IDENTICAL 400B; BATCH defaults differ-from-dataclass confirmed: posts 0.5/comments 1.0/active_hours [9..=22]=14; cfg precedence empty-object→rule + .get(field,default) on both paths; LLM-failure→rule fallback no error-propagation). S-452 `_generate_agent_config_by_rule` → `[x]` (all 6 branches, every numeric value + active_hours list byte-exact vs L912-989). **S-450 `_assign_initial_post_agents` FAIL → stays `- [~]`:** alias inner-loop break is UNCONDITIONAL (`simulation_config.rs:1843`) but Python's outer break is conditional on a match (`L789-790`) — when a poster_type matches an alias GROUP whose members are all absent, Python CONTINUES to the next matching group; Rust exits to the influence-max fallback. 3 reachable divergent cases (poster=person/only-alumni; alumni/only-student; student/only-alumni); proven at runtime (poster=person, agents=[100 alumni infl1.0, 200 official infl9.0] → Rust returned poster_agent_id=200, Python golden=100). Observable downgrade in poster_agent_id. FIX: remove the unconditional `break;` at L1843, keep the inner `break 'outer` success path, let outer `for` continue when no member found (mirror `if matched_agent_id is not None: break`). See parity.md verdict block 2026-06-17. · **SUB-CYCLE (c) RE-VERIFY — S-450 FAIL→fix→PASS 2026-06-17 (opus).** Porter removed the unconditional break; the alias scan now continues past a group whose members are all absent and resolves a later group, exactly as Python L780-790 (`break 'outer` fires only inside the matched `if let Some(agents)` block). Re-traced: poster=person/agents=[100 alumni 1.0, 200 official 9.0] now → 100 (was 200). Regression test `assign_initial_post_agents_continues_past_empty_alias_group` added; 12 S-450 tests + 648 lib tests green; S-451/S-452 undisturbed. S-450 flipped `- [x]`. **Sub-cycles (a)+(b)+(c) now ALL parity-verified; only sub-cycle (d) (generate_config S-439 + save) remains.** U-019 NOT marked done — sub-cycle (d) outstanding. · **SUB-CYCLE (d) of 4 — `generate_config` orchestration S-439 PARITY-VERIFIED 2026-06-17 (opus PASS, differential vs py:243-379).** Final symbol → `[x]`. Verified: (1) total_steps=3+`div_ceil(N/15)` ≡ `3+math.ceil(N/15)` cross-checked 0/1/14/15/16/17/30/31/45/46 incl 0-entities edge (math.ceil(0/15)=0→total_steps=3, no batch steps); (2) step numbering 1=time/2=event/3+batch_idx=agent/total_steps=platform, progress_callback `Option<&mut dyn FnMut(i64,i64,&str)>` invoked with (current_step,total_steps,msg) — tests assert seq `[1,2,3,4,5]` (n=16/17) and `[1,2,3]` (n=0), all callbacks see correct total; (3) all 8 i18n keys + common.success present en/zh with EXACT placeholders (`generatingAgentConfig {start}-{end}/{total}` w/ start=start_idx+1,end=end_idx,total=len; agentConfigResult/postAssignResult `{count}`), `t` vs `t_args` arity-correct, `t_args` substitution ≡ Python `t(key,**kwargs)`; (4) reasoning-or-success fallback both sites (time py:300/rs:2117, event py:306/rs:2129) — JSON "reasoning"-if-string else t("common.success"), both present-path + fallback-path tested; (5) batch loop start_idx=batch_idx*15/end_idx=min(start+15,len)/slice[start..end]/start_idx passed; (6) assigned_count filter `!get("poster_agent_id").map(is_null).unwrap_or(true)` ≡ Python `is not None` (absent/null excluded, present-non-null included); (7) twitter {0.4,0.3,0.3,10,0.5} & reddit NON-default {0.3,0.4,0.3,15,0.6} as explicit literals (not ..Default), enable gating both true+false tested; (8) SimulationParameters field-for-field, llm_model=self.model_name, llm_base_url=self.base_url, generation_reasoning=join(" | "), generated_at=python_isoformat_local (Python field default_factory NOT overridden — faithful); (9) no regression — edit only ADDED generate_config+tests, 662 lib tests green, simulation_config 96 green, clippy --all-targets -D warnings clean. **ALL U-019 symbols S-374..S-452 now `[x]` (72→73 symbols, zero `[≠]`). UNIT U-019 DONE.** See parity.md verdict block 2026-06-17 (d).

- [x] U-020 · `backend/app/services/simulation_ipc.py` · File-based IPC between Flask and simulation subprocesses; `CommandType` enum (INTERVIEW, BATCH_INTERVIEW, CLOSE_ENV); `CommandStatus` enum; `IPCCommand`/`IPCResponse` dataclasses with JSON round-trip; `SimulationIPCClient(sim_dir)`: `send_command(type, args, timeout=60, poll_interval=0.5)` writes `{cmd_id}.json` to `ipc_commands/`, polls `ipc_responses/` until response file appears or timeout → `TimeoutError`; `send_interview(agent_id, prompt, platform, timeout)`; `send_batch_interview(interviews, platform, timeout=120)`; `send_close_env(timeout=30)`; `check_env_alive()` reads `env_status.json`; `SimulationIPCServer(sim_dir)`: `start()`, `stop()`, `poll_commands()`, `send_response()`, `send_success()`, `send_error()`. Contract: command files cleaned up after response; race-free only for single producer/consumer per command. → `teri::services::simulation_ipc` · deps: none
  - **sub-cycle (a) VERIFIED 2026-06-17 (opus parity PASS, DECISION-15):** S-453..S-476 (24/24 `[x]`). PURE PORT, byte-exact: verifier confirmed `.value` lowercase strings (not enum-Debug), to_dict 4-key/5-key EXACT insertion order (preserve_order), result/error=None → JSON `null` never omitted, and the required-vs-tolerant from_dict split matches Python exactly (command_id/command_type/status required→Err on missing/unknown; args/timestamp/result/error tolerant via .get defaults; JSON null→None). timestamp default = shared `python_isoformat_local`. No `[≠]` (file-transport `[≠]` correctly deferred to sub-cycle b). 21 tests, 903 green. Sub-cycle (b) `SimulationIPCClient`/`SimulationIPCServer` (S-477..S-492) remains (the in-process channel map-onto, architect). PORT DETAIL ↓
  - **sub-cycle (a) PORTED 2026-06-17:** `CommandType` (S-453..S-456), `CommandStatus` (S-457..S-461), `IPCCommand` (S-462..S-468), `IPCResponse` (S-469..S-476) → `src/services/simulation_ipc.rs`. Fidelity: `.value` strings via `serde(rename_all="snake_case")`+`as_str()`; 4-key/5-key to_dict in EXACT Python insertion order (preserve_order feature); result/error=None → JSON null, never omitted; from_dict: command_id/command_type/status required (Err on missing/unknown), args/timestamp/result/error tolerant (Python .get() defaults). 21 new tests; 903 total green (was 880); clippy clean. S-453..S-476 all `[~]`.
  - **✅ U-020 COMPLETE — sub-cycle (b) VERIFIED 2026-06-17 (opus parity PASS, DECISION-16, map-onto-substrate):** S-477..S-492 (15 `[x]` + S-488 `[≠]`). file-based subprocess IPC → in-process `tokio::mpsc<IpcEnvelope>` + per-command `oneshot<IPCResponse>`; client clonable Sender, server Receiver (= the sim loop), liveness via `Arc<AtomicBool>`. Verifier adversarially traced every file artifact's consumer: the env_status.json timestamp (S-488) is read ONLY cross-process by unported U-022 (`simulation_runner.get_env_status_detail`) — within U-020's scope nothing in-process consumes it → genuine inexpressibility (same class as U-007/U-016), NOT a "won't use it" skip. All protocol behavior PORTED + differentially tested: command types/arg shapes, conditional platform-key (only when Some), timeouts as real `tokio::time::timeout` awaits (60/120/30s defaults), status/result/error + command_id round-trip, FIFO oldest-first, check_env_alive start/stop liveness, client Clone. **Fixed a cosmetic micro-divergence the gate flagged:** timeout message `{:.0}秒`→`{:?}秒` so it renders `60.0秒` matching Python's `str(float)` (not `60秒`). 35 simulation_ipc tests, 917 green, clippy clean. **ALL S-453..S-492 `[x]`/`[≠]` ⇒ U-020 COMPLETE** (a protocol types + b in-process transport). **Unblocks U-022 (social sim — deps U-020✅+U-021✅ now BOTH met), U-028/029/030, U-047.** PORT DETAIL ↓
  - **sub-cycle (b) PORTED 2026-06-17:** `SimulationIPCClient` (S-477..S-483) + `SimulationIPCServer` (S-484..S-492) + `IpcEnvelope` + `channel(buffer)` factory → appended to `src/services/simulation_ipc.rs` (DECISION-16). Transport: `tokio::sync::mpsc<IpcEnvelope>` (client tx, server rx) + per-command `tokio::sync::oneshot<IPCResponse>` embedded in envelope; liveness = `Arc<AtomicBool>` (server writes on start/stop, client reads in check_env_alive). Client is Clone (multiple callers → same channel). PORTED observables: command types + arg shapes (interview {agent_id,prompt,platform?}, batch_interview {interviews,platform?}, close_env {}); conditional platform-key insertion (only when Some, matching Python `if platform:`); timeouts as real elapsed `tokio::time::timeout` awaits with source defaults (60/120/30 s); IPCResponse status/result/error construction with command_id round-trip; FIFO oldest-first delivery (mpsc preserves send order, same as mtime sort); log lines (发送IPC命令 / 收到IPC响应); check_env_alive semantics; start()/stop() liveness transitions. `[≠]` adjudications: ipc_commands/+ipc_responses/ dirs + os.makedirs (no FS boundary in-process); env_status.json + _update_env_status timestamp (in-process AtomicBool); os.remove cleanup (oneshot self-consumes); mtime-ordered dir scan (mpsc FIFO already oldest-first); poll_interval 0.5s (channel wakes immediately; timeout real-await preserved); JSONDecodeError-retry (file-race artifact; in-process values move whole). S-488 (_update_env_status): absorbed into AtomicBool stores — no separate method needed; `[≠]` documented. 14 new tests in `ipc_transport_tests` module (covering: send_interview no/with platform args shape, conditional platform key exclusion, batch_interview args+platform, send_error→FAILED, close_env command type, check_env_alive start/stop, timeout on undraining server→TeriError::Sim, FIFO ordering of two commands, command_id UUID round-trip, client Clone concurrent send, send_success/send_error response fields, poll_commands returns None on empty). 917 total green (was 903); 0 clippy warnings. **All S-477..S-492 `[~]` — parity verification pending. Do NOT mark U-020 `- [x]`.**

- [x] U-021 · `backend/app/services/zep_graph_memory_updater.py` · Real-time Zep graph memory update from simulation agent activities; `AgentActivity` dataclass with `to_episode_text()` generating natural language descriptions for 12 action types (CREATE_POST, LIKE_POST, DISLIKE_POST, REPOST, QUOTE_POST, FOLLOW, CREATE_COMMENT, LIKE_COMMENT, DISLIKE_COMMENT, SEARCH_POSTS, SEARCH_USER, MUTE); `ZepGraphMemoryUpdater(graph_id)`: `start()` spawns daemon background thread (locale-captured before spawn); `add_activity(activity)` — skips DO_NOTHING, puts in queue; `add_activity_from_dict(data, platform)` — skips entries with `event_type` field; `_worker_loop(locale)` processes queue into per-platform buffers, flushes when `BATCH_SIZE=5` reached, then sleeps `SEND_INTERVAL=0.5s`; `_send_batch_activities` merges activities into single text episode, `client.graph.add(type="text", data=combined_text)` with `MAX_RETRIES=3`, `RETRY_DELAY=2s`; `_flush_remaining()` on stop; `get_stats()`; `ZepGraphMemoryManager` class-level dict of updaters with `create_updater`, `get_updater`, `stop_updater`, `stop_all` (idempotent via `_stop_all_done` flag). Runtime contract: daemon thread; does not block simulation; activities may be lost if process killed before flush. → `teri::services::zep_graph_memory_updater` · deps: U-007, U-005
  - **sub-cycle (a) VERIFIED 2026-06-17 (opus parity PASS, DECISION-13):** `AgentActivity` + `to_episode_text` + 12 `_describe_*` (S-493..S-514, 22/22 `[x]`) ported to NEW `src/services/graph_memory.rs`. PURE PORT, byte-exact: verifier set-diffed all 41 Chinese production literals source-vs-rust (zero on either side only), covering every full-width `：`/`，`/`「」` and the colon-vs-no-colon crux (quote_post base `帖子「` has NO `：` unlike like/dislike/repost `帖子：「`). All 4-way ladders (`both→content→author→neither`), create_comment's 5-branch (outer-on-content + inner 4-way), and the Python-falsy `or`-fallbacks (quote_content|content, query|keyword, query|username — both absent-key AND empty-string fall through) ported faithfully. `AgentActivity` is a distinct loggable record (action_type: String dispatch, action_args: serde_json Map) — NOT coupled to teri's `SocialAction` enum (faithful to MiroFish reading actions.jsonl dicts). 53 tests (850 total green).
  - **sub-cycle (b) VERIFIED 2026-06-17 (opus parity FAIL→fix→PASS, DECISION-14):** S-515..S-530 (16/16 — 13 `[x]` + 3 `[≠]` S-518/519/520). build byte-identical (23 build tests green); `extend_from_text` Pass-2 against self's full post-merge set keeps relations to pre-existing entities (no drop); true async worker (mpsc + one task owning per-platform buffers); U-050 with_locale at start(). **GATE CAUGHT a real downgrade:** get_stats().buffer_sizes returned `{}` in the empty/skip-only state vs MiroFish's seeded `{twitter:0,reddit:0}` (`__init__` L252) — FIXED (pre-seed snapshot at `new` + 3 regression tests), re-verified PASS. `[≠]` survived challenge: SEND_INTERVAL + MAX_RETRIES/RETRY_DELAY (**redundant — adapter `call_api` already retries transients**; observable failed_count++ + worker-continues IS ported) + ZEP_API_KEY keyless + Zep coreference resolution (exact-name merge, no entity dropped). 871 green. (b) PORT DETAIL below ↓
  - **sub-cycle (b) PORTED 2026-06-17:** `ZepGraphMemoryUpdater` → `GraphMemoryUpdater<L>` + `KnowledgeGraph::extend_from_text` (DECISION-14). S-515..S-530 (16 symbols). `src/graph/mod.rs`: `ExtendStats` struct, `extract_and_merge_into` private method (refactored out of build path — byte-identical regression guard in tests), `extend_from_text` public method. `src/services/graph_memory.rs`: `GraphMemoryUpdater<L: LlmClient+Send+Sync+'static>` struct with `const BATCH_SIZE=5`, `platform_display_name` (twitter→"世界1", reddit→"世界2"), `UpdaterCounters` (6 AtomicI64/AtomicUsize), `UpdaterStats` (10 serde-json fields, byte-identical Python keys), `BufferSnapshot` (`Arc<Mutex<HashMap<String,usize>>>`); methods: `new`, `start` (locale-capture before spawn per U-050), `stop` (drop tx + join + log), `add_activity` (DO_NOTHING skip before enqueue), `add_activity_from_dict` (event_type skip), `get_stats` (byte-identical JSON key set); private `worker_loop` + `flush_batch` + `update_buffer_snapshot`. `[≠]` adjudications: SEND_INTERVAL (Zep rate-limit), ZEP_API_KEY (keyless), MAX_RETRIES literal retry-loop (Zep network transient), Zep coreference entity-resolution. 10 new updater tests + 6 extend_from_text tests (868 total green, 0 clippy). Remaining: **(c) `ZepGraphMemoryManager` registry (feeds U-049 stop_all)**. **Do NOT mark U-021 `- [x]`.**
  - **✅ U-021 COMPLETE — sub-cycle (c) VERIFIED 2026-06-17 (opus parity PASS):** `ZepGraphMemoryManager` → `GraphMemoryManager<L>` (S-531..S-539, 9/9 `[x]`). class-level singleton → INSTANCE struct held in app state (forced: generic statics impossible, LlmClient not dyn-safe; observable "one registry per process, idempotent stop_all" contract preserved). **S-538 stop_all (U-049 cleanup entry):** idempotent via `compare_exchange(false,true,AcqRel,Acquire)` checked BEFORE lock (mirrors Python check-before-_lock L534); per-updater error isolation STRUCTURALLY GUARANTEED — `GraphMemoryUpdater::stop` returns `()`, no `?`, swallows panicking worker via timeout, self-logs; `drain()` clears map regardless (verifier adversarially traced: 3 updaters, middle errors → all stop, map clears). create_updater stops-old-first; `Result<()>`/`Option<UpdaterStats>` return shapes adjudicated faithful access-path adaptations (updater holds non-Clone JoinHandle behind Mutex; registry IS the access path; no caller needs the live instance). `tokio::sync::Mutex` required (async stop()/get_stats() held across guard). 9 manager_tests verified (incl. idempotent-no-double-stop, replace-stops-old). `cargo test graph_memory` 78 pass; 880 total green. **ALL S-493..S-539 `[x]`/`[≠]` ⇒ U-021 UNIT COMPLETE** (a byte-exact NL + b updater+extend_from_text + c registry). Unblocks U-022/U-045; feeds U-049 (stop_all). PORT DETAIL ↓
  - **sub-cycle (c) PORTED 2026-06-17:** `ZepGraphMemoryManager` → `GraphMemoryManager<L>` (S-531..S-539, 9 symbols). `src/services/graph_memory.rs` — appended to existing file. Struct fields: `updaters: tokio::sync::Mutex<HashMap<String, GraphMemoryUpdater<L>>>` (S-532 + S-533 fused), `stop_all_done: AtomicBool` (S-537). Methods: `new`/`Default`, `create_updater` async (stops old if present, L503-504 faithful), `get_updater` async → `Option<UpdaterStats>` (access-path decision: Mutex forbids returning &updater to caller; stats snapshot is the faithful composable equivalent), `stop_updater` async (remove+stop if present, no-op if absent), `stop_all` async (idempotent via `compare_exchange(AcqRel/Acquire)`, drain+stop all with catch-log-continue per Python L541-544, clears map — U-049 entry point), `get_all_stats` async → `HashMap<String,UpdaterStats>`. Map-onto notes: class→instance (observable singleton contract preserved), `_lock` folded into Mutex (no separate observable), `tokio::sync::Mutex` required (async methods hold lock across `.await`). 9 new tests in `manager_tests` module: create registers+starts; create replaces existing (stops old, new has fresh stats); get_all_stats returns all entries; stop_updater removes; stop_updater absent is no-op; stop_all idempotent (second call is no-op, does not double-stop); stop_all clears registry; get_updater absent returns None; Default is empty. 880 total green (was 871), 0 clippy warnings. **Sub-cycle (c) PORTED — parity verification pending. Do NOT mark U-021 `- [x]`.**

---

## Layer 4 — Simulation Runner (subprocess orchestration)

- [ ] U-022 · `backend/app/services/simulation_runner.py:SimulationRunner` · Class-level state: `_run_states: Dict[str, SimulationRunState]`, `_processes: Dict[str, subprocess.Popen]`, `_action_queues: Dict[str, Queue]`, `_monitor_threads: Dict[str, Thread]`, `_stdout_files: Dict[str, File]`, `_graph_memory_enabled: Dict[str, bool]`; `RunnerStatus` enum; `AgentAction`, `RoundSummary`, `SimulationRunState` dataclasses; `start_simulation(sim_id, platform, max_rounds, enable_graph_memory_update, graph_id)` → spawns subprocess (run_twitter/reddit/parallel_simulation.py) + daemon monitor thread; `_monitor_simulation(sim_id, platform)` polls JSONL action log files every 2s by file position (`_read_action_log` seek pattern), fires `ZepGraphMemoryUpdater.add_activity_from_dict` if graph memory enabled; detects `simulation_end` event to mark completed; `_check_all_platforms_completed(sim_id)` checks both platforms when dual; `_terminate_process(process)` cross-platform: Windows → `taskkill /F /IM /T`, Unix → `os.killpg(pgid, SIGTERM)` then SIGKILL after 5s; `stop_simulation(sim_id)` terminates + cleanup; `get_actions(sim_id, platform, limit, offset, agent_id, round_num)` reads actions from file; `get_timeline(sim_id, round_summary)` by rounds; `get_agent_stats(sim_id)` action type distribution; `check_env_alive(sim_id)` delegates to `SimulationIPCClient.check_env_alive`; `interview_agent(sim_id, agent_id, prompt, platform, timeout)` via IPC; `interview_agents_batch(sim_id, interviews, platform, timeout=120)` via IPC; `interview_all_agents(sim_id, prompt, platform, timeout)` reads profiles + batch interviews all; `get_interview_history(sim_id, platform, agent_id)` reads from SQLite `twitter_simulation.db`/`reddit_simulation.db`; `close_simulation_env(sim_id, timeout)` via IPC; `register_cleanup()` installs SIGTERM/SIGINT/SIGHUP handlers + atexit: all call `cleanup_all_simulations()`; `cleanup_all_simulations()` terminates all running processes. Runtime contracts: monitor threads are daemon=True (die with process); subprocess pgid tracked for process group kill; JSONL polling by file position avoids re-reading; dual-platform checks require both twitter+reddit streams. → `teri::services::simulation_runner` · deps: U-020, U-021, U-010, U-005

---

## Layer 5 — Simulation Manager (preparation pipeline)

- [x] U-023 · `backend/app/services/simulation_manager.py:SimulationManager` · FS-backed (NOT singleton); `SimulationStatus` enum (CREATED, PREPARING, READY, FAILED); `PlatformType` enum (TWITTER, REDDIT, BOTH); `SimulationState` dataclass with `to_dict()`/`to_simple_dict()`; `create_simulation(project_id, graph_id, enable_twitter, enable_reddit)` → uuid sim_id, creates `uploads/simulations/{sim_id}/` dir, saves `state.json`; `prepare_simulation(sim_id, entity_types, use_llm_for_profiles=True, parallel_profile_count=1, force_regenerate=False)` → 4-stage async task: (1) `ZepEntityReader.filter_defined_entities` reads entities, (2) `OasisProfileGenerator.generate_profiles_from_entities` generates profiles (concurrent if `parallel_profile_count>1` via `asyncio.gather`), (3) `SimulationConfigGenerator.generate_config` generates OASIS config, (4) saves state READY; `get_profiles(sim_id, platform)` reads JSON/CSV; `get_simulation_config(sim_id)` reads JSON. Contract: preparation is long-running (minutes); task_id returned immediately; `force_regenerate=False` skips already-completed stages. Error: marks state FAILED on exception, stores error in state.json. → `teri::services::simulation_manager` · deps: U-016, U-018, U-019, U-012, U-005
  - **sub-cycle (b) VERIFIED 2026-06-17 (parity PASS):** state types S-636..S-667 parity-proven (differential vs `simulation_manager.py:25-112`); 32/32 symbols `- [x]`. **Ledger-summary corrections (source authoritative):** `SimulationStatus` has **8** variants `created,preparing,ready,running,paused,stopped,completed,failed` (this row's "CREATED, PREPARING, READY, FAILED" undercounted); `PlatformType` has **2** variants `twitter,reddit` only — **no `BOTH`** (this row's "TWITTER, REDDIT, BOTH" was wrong). Remaining: sub-cycle (c) `SimulationManager` (S-668+) + (d) `create_simulation`/`prepare_simulation`/`get_profiles`/`get_simulation_config` — all still `- [ ]`. **Do NOT mark U-023 `- [x]`.**
  - **sub-cycle (c) VERIFIED 2026-06-17 (parity PASS):** `SimulationManager` struct + FS persistence + getters S-668..S-674, S-676..S-680 parity-proven (differential vs `simulation_manager.py:115-529`, both sides cited in `findings/parity.md`); 12/12 in-scope symbols `- [x]`. `cargo test --lib services::simulation_manager` = 39 passed. **S-680 `get_run_instructions` ships as adjudicated `[≠]`-partial:** path fields (`simulation_dir`, `config_file`) ported; `scripts_dir`/`commands`/`instructions` (Python `run_*_simulation.py` + `conda activate` strings) genuinely INEXPRESSIBLE in teri's substrate (teri runs `SimEngine::run` in-process, confirmed `src/sim/mod.rs:493`; the runner scripts are separate native units U-028/U-029/U-030, all `- [ ]`). **CARRY-FORWARD GATE → U-026:** the API field `run_instructions` is served to the frontend (`api/simulation.py:772`, when status==READY); when U-026 (`teri::api::simulation`) is ported, the parity gate MUST verify teri emits *native* run-guidance (the `teri run`/`SimEngine::run` invocation), NOT merely the static `substrate_note`, or the "how to run" contract is downgraded. Remaining: **only sub-cycle (d) `prepare_simulation` (S-675)**. **Do NOT mark U-023 `- [x]`.**
  - **sub-cycle (c) PORTED 2026-06-17 (parity pending):** `SimulationManager` struct (S-668..S-674, S-676..S-680) ported to `src/services/simulation_manager.rs`. Interior mutability: `Mutex<HashMap<String,SimulationState>>` (axum-compatible). SIMULATION_DATA_DIR→`sim_data_dir` from `config.oasis_simulation_data_dir`. All 5 getters + `create_simulation` + FS persistence methods ported with exact Python behavior (cache-first, updated_at-before-write, raise-vs-None-vs-[] error distinctions, id format `sim_`+12hex, invalid-status→Err). S-680 get_run_instructions: structural fields ported; `commands`/`instructions`/`scripts_dir` marked `[≠]`-substrate (OASIS Python subprocess commands; teri runs SimEngine in-process — genuinely inexpressible, NOT "won't use"). 21 new tests (788 total, 0 failing). cargo build + clippy --all-targets -D warnings: green. Remaining: sub-cycle (d) `prepare_simulation` (S-675). **Do NOT mark U-023 `- [x]`.**
  - **✅ U-023 COMPLETE — sub-cycle (d) VERIFIED 2026-06-17 (opus parity PASS):** S-675 `prepare_simulation` + re-opened S-367 `generate_profiles_from_entities` both parity-proven (differential vs `simulation_manager.py:230-458` + `oasis_profile_generator.py:851-1014`; verdict in `findings/parity.md`). Verified by-running: 7/7 prepare tests, 38/38 oasis_profile_export (incl. 2 determinism tests), 4/4 t_args, full lib 786 green, no regression. Verified by-reading: NO spawn/task_id smuggled (correctly deferred to U-026); NO `force_regenerate` (source has none — handoff-summary corrections: the row text above mentioning `parallel_profile_count=1`/`force_regenerate`/`asyncio.gather`/4-stage-async-task was the cartographer's GUESS; SOURCE is authoritative — sync method, default `parallel_profile_count=3`, no force_regenerate); 0-entity→Ok(FAILED) vs exception→Err(FAILED) distinct + tested; all 11 progress callbacks reproduced (exact stage/percentage/current/total); S-367 determinism crux proven (indexed-slot writes, byte-for-byte equal across parallel_count ∈ {1,3,10}, `buffer_unordered(parallel_count.max(1))` bounds concurrency); per-entity fallback + realtime-save-non-fatal preserved; bio/persona kept distinct (avoids the cycle-8/9 collapse downgrade class). `[≠]` keepers adjudicated & survived: S-368 (stdout console artifact, non-contractual), S-680 (OASIS subprocess/conda strings inexpressible — U-026 carry-forward gate stands). **ALL S-636..S-680 now `[x]`/`[≠]` ⇒ U-023 UNIT COMPLETE.**
  - **sub-cycle (d) PORTED 2026-06-17 (parity pending):** `PrepareProgress<'a>` struct + `prepare_simulation<L: LlmClient>` (S-675) ported to `src/services/simulation_manager.rs`. All 4 stages faithful to Python L230-458: (1) KnowledgeGraphEntityReader::new(graph).filter_defined_entities → entities_count/entity_types; (2) generate_profiles_from_entities with live parallel_count knob via buffer_unordered → reddit_profiles.json + twitter_profiles.csv (two independent `if` branches, not `elif`); (3) SimulationConfigGenerator::new(llm).generate_config → simulation_config.json + config_reasoning; (4) status READY + save + Ok(state). Zero-entity path: Ok(state) with status FAILED (NOT Err — Python `return state`). Exception wrapper: try_stage! macro → FAILED + save + Err (Python `except: raise`). Raw-ptr trick (`cb_ptr: *mut Option<&mut dyn FnMut(PrepareProgress<'_>)>`) resolves profile_progress double-borrow without unsafe aliasing (buffer_unordered polls on current task). 7 new tests in `prepare_tests` module (3 missing-id/zero-entity/exception, 2 happy-path Reddit+Twitter, 1 progress-callback stages, 1 state-fields-populated); 797 total tests passing. Also re-touched `oasis_profile_export::generate_profiles_from_entities`: sequential→buffer_unordered live parallel_count knob (DECISION-11 §2); 3 determinism tests added (S-367 drops to `[~]` pending re-verify by opus gate). cargo build + clippy --all-targets -D warnings: green. **Do NOT mark U-023 `- [x]`.**

---

## Layer 6 — Report Agent

- [ ] U-024 · `backend/app/services/report_agent.py:ReportAgent` · ReACT-pattern (Reasoning + Acting) multi-section report generator; `ReportLogger` writes JSONL `agent_log.jsonl` with actions (report_start, planning_start, planning_context, planning_complete, section_start, react_thought, tool_call, tool_result, llm_response, section_content, section_complete, report_complete, error); `ReportConsoleLogger` attaches file handler to named loggers, writes `console_log.txt`; `ReportStatus` enum; `ReportSection`/`ReportOutline`/`Report` dataclasses; `ReportManager` FS-based report persistence (separate class in same file): saves `meta.json`, `outline.json`, `progress.json`, `section_N.md`, `full_report.md`, `agent_log.jsonl`, `console_log.txt`; `plan_outline(progress_callback)` calls `ZepToolsService.get_simulation_context` + LLM JSON response for 2-5 section outline with fallback; `_generate_section_react(section, outline, prev_sections, progress_callback, section_index)` runs up to 5 iterations: parses `<tool_call>{...}</tool_call>` blocks (with bare-JSON fallback), executes one tool per iteration (insight_forge, panorama_search, quick_search, interview_agents via `_execute_tool`), enforces min 3 tool calls, max 5 tool calls, conflict detection (simultaneous tool+FinalAnswer), forces output on max iterations; `generate_report(progress_callback, report_id)` orchestrates planning + section generation + final assembly; `chat(simulation_id, message, chat_history, report_id)` tool-assisted chat against generated report (max 2 tool calls). Runtime contract: generation is synchronous (called from background thread); `ReportConsoleLogger` dynamically attaches/detaches logging handlers — must be closed explicitly. → `teri::services::report_agent` · deps: U-017, U-008, U-005

---

## Layer 7 — HTTP API Blueprints

- [ ] U-025 · `backend/app/api/graph.py` · Blueprint `graph_bp` at `/api/graph`; routes: `POST /ontology/generate` (multipart: files + simulation_requirement + project_name → `OntologyGenerator.generate` → saves to project + starts `GraphBuilderService.build_graph_async`); `POST /build` (json: project_id, graph_name → builds graph); `GET /task/<task_id>` → TaskManager status; `GET /data/<graph_id>` → `GraphBuilderService.get_graph_data`; `GET /project/<project_id>`, `GET /project/list`, `DELETE /project/<project_id>`, `POST /project/reset`; `DELETE /graph/<graph_id>` → `GraphBuilderService.delete_graph`; `allowed_file(filename)` helper. Contract: multipart upload creates project dir; accepted file types per `Config.ALLOWED_EXTENSIONS`; max content length 50MB enforced by Flask. Background thread for graph building captures locale from request context before spawn. Error paths: 400 on missing params, 500 on service errors. → `teri::api::graph` · deps: U-011, U-012, U-014, U-015, U-005

- [ ] U-026 · `backend/app/api/simulation.py` · Blueprint `simulation_bp` at `/api/simulation`; 30+ routes including: `POST /create`, `POST /prepare` (async task), `POST /prepare/status`, `GET /<id>`, `GET /<id>/profiles`, `GET /<id>/profiles/realtime`, `GET /<id>/config`, `GET /<id>/config/realtime`, `GET /list`, `POST /start` (launch subprocess), `POST /stop`, `GET /<id>/run-status`, `GET /<id>/run-status/detail`, `GET /<id>/actions`, `GET /<id>/timeline`, `GET /<id>/agent-stats`, `GET /<id>/posts`, `GET /<id>/comments`, `GET /interview/<agent_id>`, `POST /interview/batch`, `POST /interview/all`, `GET /interview/history`, `POST /env-status`, `POST /close-env`, `GET /history`; helpers: `_check_simulation_prepared(sim_id)` checks `state.json` status and required files; `optimize_interview_prompt(prompt)` prefixes interview prompt to prevent tool calls; `get_graph_entities`, `get_entity_detail`, `get_entities_by_type` for entity browsing. Locale captured from request `Accept-Language`, propagated to background tasks. Error: 400/404/500. → `teri::api::simulation` · deps: U-022, U-023, U-011, U-016

- [ ] U-027 · `backend/app/api/report.py` · Blueprint `report_bp` at `/api/report`; routes: `POST /generate` (async, pre-generates report_id before task start so frontend gets it immediately), `GET /generate/status`, `GET /<report_id>`, `GET /by-simulation/<sim_id>`, `GET /list`, `GET /<report_id>/download`, `DELETE /<report_id>`, `POST /chat`, `GET /<report_id>/progress`, `GET /<report_id>/sections`, `GET /<report_id>/sections/<n>`, `GET /<report_id>/status`, `GET /<report_id>/agent-log` (incremental, `from_line` param), `GET /<report_id>/agent-log/stream`, `GET /<report_id>/console-log` (incremental), `GET /<report_id>/console-log/stream`, `GET /graph-search`, `GET /graph-statistics`; background generate thread: `run_generate(sim_id, report_id, force_regenerate, locale)` creates `ReportAgent` + calls `generate_report`. Runtime contract: report_id returned before generation completes; `agent_log.jsonl` and `console_log.txt` written incrementally; `from_line` polling for log streaming. → `teri::api::report` · deps: U-024, U-017

---

## Layer 8 — Simulation Scripts (subprocess executables)

- [ ] U-028 · `backend/scripts/run_twitter_simulation.py:TwitterSimulationRunner` · Runs as subprocess; reads `--config simulation_config.json`; `setup_oasis_logging(log_dir)` configures OASIS library loggers to files; `UnicodeFormatter` decodes Unicode escapes in log output; `MaxTokensWarningFilter` suppresses camel-ai max_tokens warnings; `IPCHandler(sim_dir)`: polls `ipc_commands/` every 0.5s, handles INTERVIEW/BATCH_INTERVIEW/CLOSE_ENV commands, calls OASIS `env.interview_agent(id, prompt)`; `TwitterSimulationRunner.__init__` loads config, creates OASIS Twitter environment; `_create_model()` configures OASIS LLM model; `_get_active_agents_for_round(hour)` filters by agent activity schedule; `run()` async main loop: round-by-round agent actions, logs via `PlatformActionLogger`, after all rounds enters wait-for-commands mode (unless `--no-wait`); `main()` / `setup_signal_handlers()` / `signal_handler()`. Runtime contracts: asyncio event loop; signal handlers set SIGTERM/SIGINT/SIGHUP to trigger graceful shutdown; `env_status.json` written by IPCHandler with `{"status":"alive"}`; SQLite DB written by OASIS to `twitter_simulation.db`; blocking IPC poll loop runs concurrently with simulation (asyncio task). → `teri::scripts::twitter_simulation` · deps: U-010, U-020

- [ ] U-029 · `backend/scripts/run_reddit_simulation.py:RedditSimulationRunner` · Mirror of U-028 for Reddit platform; same structure: `RedditSimulationRunner`, `IPCHandler`, `setup_oasis_logging`, signal handling; writes `reddit_simulation.db`. → `teri::scripts::reddit_simulation` · deps: U-010, U-020

- [ ] U-030 · `backend/scripts/run_parallel_simulation.py` · Runs both Twitter AND Reddit platforms concurrently in same process via asyncio; `PlatformSimulation` dataclass holds env + runner per platform; `run_twitter_simulation(config, action_logger, sim_dir, ipc_handler)` and `run_reddit_simulation(...)` as coroutines; `ParallelIPCHandler` handles interview commands for both platforms simultaneously via `_interview_single_platform`; `fetch_new_actions_from_db(db_path, last_offset)` polls SQLite for new action rows; `_enrich_action_context` + `_get_post_info` + `_get_user_name` + `_get_comment_info` enriches action_args with post/user content from DB before logging to JSONL; `create_model(config)` configures OASIS model; `get_active_agents_for_round(config, hour)`. Runtime contracts: both platforms run as `asyncio.gather` tasks; IPC handler polls across both envs; `_utf8_open` helper for Windows UTF-8 file I/O; action enrichment happens before JSONL write so `action_args` carries full context. → `teri::scripts::parallel_simulation` · deps: U-010, U-020

---

## Layer 9 — Frontend (Vue 3 + Vite SPA)

- [ ] U-031 · `frontend/src/api/index.js` · Axios instance: `baseURL = VITE_API_BASE_URL || http://localhost:5001`, `timeout=300000` (5min); request interceptor injects `Accept-Language: {locale}` header; response interceptor: unwraps `res.data`, rejects if `!res.success`; `requestWithRetry(fn, maxRetries=3, delay=1000)` with exponential backoff. → `teri::frontend::api_client` · deps: none

- [ ] U-032 · `frontend/src/api/graph.js` · Graph API client functions: `generateOntology(formData)` (multipart), `buildGraph(data)`, `getTaskStatus(taskId)`, `getGraphData(graphId)`, `getProject(projectId)`. All use `requestWithRetry`. → `teri::frontend::api::graph` · deps: U-031

- [ ] U-033 · `frontend/src/api/simulation.js` · Simulation API client: `createSimulation`, `prepareSimulation`, `getPrepareStatus`, `getSimulation`, `getSimulationProfiles`, `getSimulationProfilesRealtime`, `getSimulationConfig`, `getSimulationConfigRealtime`, `listSimulations`, `startSimulation`, `stopSimulation`, `getRunStatus`, `getRunStatusDetail`, `getSimulationPosts`, `getSimulationTimeline`, `getAgentStats`, `getSimulationActions`, `closeSimulationEnv`, `getEnvStatus`, `interviewAgents`, `getSimulationHistory`. → `teri::frontend::api::simulation` · deps: U-031

- [ ] U-034 · `frontend/src/api/report.js` · Report API client: `generateReport`, `getReportStatus`, `getAgentLog(reportId, fromLine=0)`, `getConsoleLog(reportId, fromLine=0)`, `getReport`, `chatWithReport`. → `teri::frontend::api::report` · deps: U-031

- [ ] U-035 · `frontend/src/store/pendingUpload.js` · Vue reactive store (not Pinia): `state = reactive({files, simulationRequirement, isPending})`; `setPendingUpload(files, requirement)`, `getPendingUpload()`, `clearPendingUpload()`. Used for cross-page file transfer (Home → Process without re-upload). → `teri::frontend::store::pending_upload` · deps: none

- [ ] U-036 · `frontend/src/i18n/index.js` · vue-i18n setup: glob-imports all `locales/*.json` except `languages.json`, creates i18n with `legacy=false`, reads `savedLocale` from localStorage (default `'zh'`), fallback `'zh'`; exports `availableLocales`. → `teri::frontend::i18n` · deps: none

- [ ] U-037 · `frontend/src/router/index.js` · Vue Router: 6 named routes: `Home(/)`, `Process(/process/:projectId)`, `Simulation(/simulation/:simulationId)`, `SimulationRun(/simulation/:simulationId/start)`, `Report(/report/:reportId)`, `Interaction(/interaction/:reportId)`. HTML5 history mode. → `teri::frontend::router` · deps: none

- [ ] U-038 · `frontend/src/views/Home.vue` · Landing page: file upload + simulation requirement input; on submit: stores files in pendingUpload store, navigates to `/process/:projectId` after API `generateOntology` call. → `teri::frontend::views::Home` · deps: U-031, U-032, U-035

- [ ] U-039 · `frontend/src/views/MainView.vue` · Step 1-3 workflow (graph build + simulation create/prepare): polls task status; shows ontology, graph progress, entity list; navigates to `/simulation/:simulationId`. → `teri::frontend::views::MainView` · deps: U-031, U-032, U-033

- [ ] U-040 · `frontend/src/views/SimulationView.vue` · Simulation configuration review: shows profiles + config; navigate to `/simulation/:simulationId/start`. → `teri::frontend::views::SimulationView` · deps: U-031, U-033

- [ ] U-041 · `frontend/src/views/SimulationRunView.vue` · Live simulation: start/stop controls, real-time action feed polling `getRunStatus/Detail`, timeline, agent stats; triggers report generation on completion. → `teri::frontend::views::SimulationRunView` · deps: U-031, U-033, U-034

- [ ] U-042 · `frontend/src/views/ReportView.vue` · Report display: polls `getReportStatus`, shows outline + incrementally loaded sections via `agent-log` polling (`from_line`); final markdown render. → `teri::frontend::views::ReportView` · deps: U-031, U-034

- [ ] U-043 · `frontend/src/views/InteractionView.vue` · Report chat: `chatWithReport` calls with chat history. → `teri::frontend::views::InteractionView` · deps: U-031, U-034

- [ ] U-044 · `frontend/src/main.js + App.vue` · Vue app bootstrap: mounts app with router + i18n plugins. → `teri::frontend::app` · deps: U-036, U-037

---

## Layer 10 — Runtime / Concurrency / Signal Contracts (first-class rows)

- [ ] U-045 · RUNTIME: Background thread management · Three categories of daemon threads: (1) task threads (`build_graph_async`, `prepare_simulation`, `generate_report`) — one per operation, captures locale before spawn, reports progress via TaskManager or file; (2) monitor threads (`_monitor_simulation`) — one per active simulation, polls JSONL file by offset every 2s, writes to `ZepGraphMemoryUpdater` queue; (3) Zep memory worker thread — one per simulation with graph memory enabled, processes queue in BATCH_SIZE=5 groups. All daemon=True. Contract: no join on cleanup; signal handlers call `cleanup_all_simulations()` which terminates subprocesses. Parity risk: sequential polling → must not become blocking in Rust; daemon thread semantics must be preserved. → `teri::runtime::thread_manager` · deps: U-022, U-021

- [ ] U-046 · RUNTIME: Subprocess lifecycle · `SimulationRunner.start_simulation` spawns `python run_{platform}_simulation.py --config {path}` as `subprocess.Popen`; stdout redirected to `{sim_dir}/stdout.log`; process group created via `os.setsid()` (Unix) for group kill; `_terminate_process` sends SIGTERM to process group, then SIGKILL after 5s if still alive; Windows: `taskkill /F /IM /T`; subprocess inherits env (LLM_API_KEY etc. must be in env). Contract: exactly one subprocess per simulation_id; monitor thread starts after subprocess. Parity risk: Rust port must replicate pgid-based kill, graceful shutdown window. → `teri::runtime::subprocess_lifecycle` · deps: U-022

- [ ] U-047 · RUNTIME: File-based IPC protocol · Flask ↔ simulation subprocess communicate via `{sim_dir}/ipc_commands/` and `{sim_dir}/ipc_responses/` directories; `{command_id}.json` files; client polls response dir every 0.5s; command file deleted on response; `env_status.json` for liveness check; timeout raises `TimeoutError`. Contract: single producer/consumer per command; interview timeout=60s default (batch=120s). Parity risk: file polling must be async-safe in Rust. → `teri::runtime::ipc_protocol` · deps: U-020

- [x] U-048 · RUNTIME: JSONL action log streaming · [PARITY-PASS cycle-7: in-band sim_end terminal signal — SimCompletion+subscribe_completion (watch, late-subscriber-safe via _completion_anchor) + TickStreamEvent::sim_end; fires after last snapshot, total_ticks==executed-count; mirrors action_logger.log_simulation_end / simulation_runner monitor; SSE-handler consumer wiring correctly deferred to U-026 (not a gap)] · Action logs written by simulation subprocess to `{sim_dir}/{platform}/actions.jsonl` in real-time; Flask monitor thread reads by file position (seek + readline), parses each line; format: `{"round":N, "timestamp":..., "agent_id":N, "agent_name":..., "action_type":..., "action_args":{...}, ...}` for actions; `{"event_type":"round_end"/"simulation_end", ...}` for events. Enriched `action_args` in parallel script includes post/comment content. Contract: monitor thread never re-reads already-processed lines; file position tracked in `_run_states`. Parity risk: tail-like streaming must preserve ordering; no line lost on rotation. → `teri::runtime::action_log_streaming` · deps: U-022

- [ ] U-049 · RUNTIME: Signal/graceful shutdown · `register_cleanup()` installs handlers for SIGTERM, SIGINT, SIGHUP on the Flask process; all call `cleanup_all_simulations()` (terminates all subprocesses + Zep updaters); `atexit` registered as backup; `ZepGraphMemoryManager.stop_all()` called with `_stop_all_done` idempotency flag. Contract: handlers are process-level; safe to call multiple times. → `teri::runtime::signal_handling` · deps: U-022, U-021

- [ ] U-050 · RUNTIME: Locale propagation into threads · Flask's `Accept-Language` header sets thread-local locale; before any `threading.Thread` spawn, caller captures `current_locale = get_locale()` and passes it as arg; worker calls `set_locale(locale)` before any `t()` calls. Affected: `build_graph_async`, `prepare_simulation`, `generate_report`, `ZepGraphMemoryUpdater.start()`. Contract: `t()` in background threads must use worker-set locale, not Flask request context. Parity risk: Rust must replicate locale threading (task-local storage). → `teri::runtime::locale_threading` · deps: U-005

---

## Sweep items (deferred / partially inventoried)

- [ ] SWEEP-1 · `frontend/src/components/*.vue` — 6 step-workflow components (StepFile, StepGraph, StepSimulation, StepConfig, StepRun, StepReport or equivalent); structural inventory only; full behavioral contract deferred to porter reading source. · deps: U-037–U-043

- [ ] SWEEP-2 · `locales/*.json` (en.json + zh.json) — i18n translation keys; all keys used by `t()` calls in backend AND frontend must be ported (Rust i18n crate or compile-time strings). · deps: U-005, U-036

- [ ] SWEEP-3 · OASIS library contract (camel-oasis==0.2.5, camel-ai==0.2.78) — MiroFish spawns OASIS as subprocess; teri does not need to re-implement OASIS, but must configure subprocess interface correctly; library is Python-only. This is a MAP-ONTO decision: teri calls Python OASIS via subprocess, same as MiroFish. · deps: U-028, U-029, U-030

- [ ] SWEEP-4 · `backend/scripts/test_profile_format.py` — utility test script, 2 symbols; behavior not part of production surface. DEFERRED: not a required parity target. · deps: none
