# HANDOFF — MiroFish → teri rust-port-merge

**Resume signal for the autonomous /loop.** Read this + `loop_state.md`, then continue at ITERATE.

## 🔁 GIT / PR DISCIPLINE (owner-mandated 2026-06-17 — DO NOT REGRESS)
- **PUSH `port/mirofish` TO `origin` EVERY SESSION.** Owner flagged unpushed local-only work as DANGEROUS (38 commits had accumulated unpushed). After each session's commits, `git push origin port/mirofish` (fast-forward; the pre-push preflight runs fmt+clippy — keep them green). **Run `cargo fmt` before pushing** — the porter cycles run clippy but NOT fmt, and CI's Format gate (and the pre-push hook) will block on unformatted code (fixed the whole branch once in commit 1afbe0c).
- **PR #5 `port/mirofish → develop` is OPEN and HELD** (https://github.com/FlexNetOS/teri/pull/5). Owner decision (11th resume): keep it open for visibility/CI/backup, **merge to develop only at port-DONE (100% units)** per the loop's DONE-gate — do NOT arm auto-merge mid-port (no CI checks exist on the repo, so auto-merge would merge the incomplete port immediately). Update the PR body as units complete.
- **develop was REPAIRED** (PR #6 merged 2026-06-17 → develop tip `7c354a5`): the bad PR #4 merge (`c894de8`, non-compiling dup `api_key`/dead block) is fixed; develop now compiles. `port/mirofish` already contains the same repair (9836238) — expect a trivial config.rs/main.rs auto-resolve when PR #5 eventually merges.
- Note: gh defaults fork PRs to the upstream parent — always use **`gh pr ... -R FlexNetOS/teri`**.

## Where we are  (SESSION HAND OFF — 2026-06-17 [11th resume]; resume cold from here)
- **Phase:** ITERATE in progress. **22/50 units `[x]` + U-003 `[~]`**. teri **917 tests green** (clippy `--all-targets -D warnings` clean). ~400/1087 symbols `[x]`.
- **THIS SESSION (2026-06-17, 11th resume) — 2 ITERATE cycles → U-020 COMPLETE (both opus-gated PASS; stopped at 2 of budget-3, see "🔴 FIRST ACTION" for why):**
  - **cycle1 (6233ae8): U-020 (a) IPC protocol types (S-453..S-476, 24/24 `[x]`)** — opus PASS (DECISION-15). NEW `src/services/simulation_ipc.rs`. PURE PORT byte-exact: `CommandType`(interview/batch_interview/close_env) + `CommandStatus`(pending/processing/completed/failed) string enums; `IPCCommand`/`IPCResponse` to_dict/from_dict — `.value` lowercase strings, 4-key/5-key EXACT insertion order (preserve_order), result/error=None → JSON `null` never omitted, required-vs-tolerant from_dict split matches Python. 21 tests.
  - **cycle2 (8613d11): U-020 (b) `SimulationIPCClient` + `SimulationIPCServer` (S-477..S-492, 15 `[x]` + S-488 `[≠]`) → U-020 COMPLETE** — opus PASS (DECISION-16, architect). **Map-onto-substrate (file-based subprocess IPC → in-process):** `channel(buffer) -> (Client, Server)`; transport = `tokio::mpsc<IpcEnvelope>` (client clonable Sender, server Receiver = the sim loop) + per-command `oneshot<IPCResponse>` embedded in the envelope (automatic reply correlation); liveness = `Arc<AtomicBool>` replacing env_status.json. PORTED protocol: command types/arg shapes, conditional platform-key (only when `Some`), timeouts as REAL `tokio::time::timeout` awaits (60/120/30s defaults), status/result/error + command_id round-trip, FIFO oldest-first, check_env_alive start/stop. **`[≠]` (all rest on the LOCKED OASIS-subprocess→in-process-SimEngine substrate — no FS boundary):** ipc dirs+makedirs, env_status.json+_update_env_status timestamp (S-488 — verifier traced: read ONLY cross-process by unported U-022), os.remove cleanup, mtime-scan, poll_interval, JSONDecodeError-retry. Fixed a cosmetic gate-flag (timeout msg `{:.0}秒`→`{:?}秒` → renders `60.0秒` matching Python `str(float)`). 35 simulation_ipc tests.
- **THIS SESSION (2026-06-17, 10th resume) — 3 ITERATE cycles (CYCLE BUDGET), all opus-gated → U-021 COMPLETE across 3 sub-cycles (47 symbols):**
  - **cycle1 (723f5f3): U-021 (a) `AgentActivity` + `to_episode_text` + 12 `_describe_*` (S-493..S-514, 22/22 `[x]`)** — opus PASS (DECISION-13). NEW `src/services/graph_memory.rs`. PURE PORT, byte-exact (verifier set-diffed all 41 Chinese literals source-vs-rust); the 4-way ladders (`both→content→author→neither`), create_comment 5-branch, the colon-vs-no-colon crux (quote_post base `帖子「` has NO `：`), and the Python-falsy `or`-fallbacks (quote_content|content, query|keyword|username — absent-key AND empty-string fall through). `AgentActivity` is a distinct loggable record (action_type: String dispatch, action_args: serde_json Map) — NOT coupled to `SocialAction`. 53 tests.
  - **cycle2 (fa8fd8b): U-021 (b) `ZepGraphMemoryUpdater` (S-515..S-530, 13 `[x]` + 3 `[≠]`)** — opus **FAIL→fix→PASS** (DECISION-14, architect). **Map-onto-substrate (Zep `graph.add(type=text)` → teri in-process LLM extraction):** NEW additive `KnowledgeGraph::extend_from_text(&mut self, text, &L) -> ExtendStats` (`src/graph/mod.rs`) — refactored the per-chunk extract+merge body of `build_with_progress_and_ontology` into private `extract_and_merge_into(&mut self)`; `build*` call it on a fresh graph (**byte-identical — 23 U-015 build tests green**), `extend_from_text` calls it on `self`. Merge by EXACT case-sensitive name; **Pass-2 relations resolve against `self`'s FULL post-merge entity set → a relation to a pre-existing entity is NOT dropped** (why extend-into-self beats throwaway-subgraph). `GraphMemoryUpdater<L>`: `Arc<tokio::Mutex<KnowledgeGraph>>` + Arc-shared `LlmClient`; **TRUE async worker** (mpsc + ONE spawned task owning per-platform buffers, no lock); batch-at-exactly-5; `combined_text=join("\n")`; DO_NOTHING/`event_type` skips; `_flush_remaining` no-loss; get_stats 10-key. **U-050 site: start() captures get_locale() before spawn + with_locale-wraps the worker.** **GATE CAUGHT a real downgrade:** `get_stats().buffer_sizes` returned `{}` in the empty/skip-only state vs MiroFish's seeded `{twitter:0,reddit:0}` — fixed (pre-seed snapshot at `new` + 3 regression tests). **3 `[≠]` survived:** SEND_INTERVAL + MAX_RETRIES/RETRY_DELAY (**redundant — adapter `call_api` already retries transients**; the observable `failed_count++`+worker-continues IS ported) + ZEP_API_KEY keyless + Zep coreference resolution (exact-name merge, NO entity dropped — inexpressible).
  - **cycle3 (aa5c2e0): U-021 (c) `ZepGraphMemoryManager` (S-531..S-539, 9/9 `[x]`) → U-021 COMPLETE** — opus PASS. class-level singleton → INSTANCE struct `GraphMemoryManager<L>` held in app state (forced: generic statics impossible, `LlmClient` not dyn-safe; observable "one registry per process, idempotent stop_all" preserved). **stop_all** idempotent via `compare_exchange` checked BEFORE the lock + per-updater error isolation STRUCTURALLY GUARANTEED (`GraphMemoryUpdater::stop` infallible/no `?`, `drain()` clears regardless — a failing updater never aborts cleanup) — the **U-049 cleanup entry point**. create_updater stops-old-first; `Result<()>`/`Option<UpdaterStats>` return shapes = faithful access-path adaptations (updater holds non-Clone JoinHandle behind a Mutex; the registry IS the access path). 9 manager_tests.
- **THIS SESSION (2026-06-17, 9th resume) — 2 ITERATE cycles, both full-unit COMPLETIONS, both opus-gated PASS (stopped at 2 of budget 3 — see "🔴 FIRST ACTION" for why + what's next):**
  - **cycle1 (a94658a): U-023 sub-cycle (d) `prepare_simulation` (S-675) → U-023 COMPLETE** (DECISION-11). `src/services/simulation_manager.rs`: `PrepareProgress<'a>` + `prepare_simulation<L: LlmClient>(...) -> Result<SimulationState>`. **3 source-authoritative corrections to the prior handoff's GUESSES:** (a) **NO `task_id`/`tokio::spawn` here** — source `prepare_simulation` is synchronous returning `SimulationState`; the task/thread/`task_id`-immediately wrapping is in `api/simulation.py:360-618` = **U-026** (carry-forward); teri port is a plain `async fn`. (b) **NO `force_regenerate`** — source signature has none, every stage always runs. (c) the cartographer ledger-row text (`parallel_profile_count=1`/`force_regenerate`/`asyncio.gather`) was a guess; SOURCE = sync, default `parallel_profile_count=3`. The 4 stages (reading→profiles→config→READY) ported faithfully; **0-entity → `Ok(state)` with FAILED status** vs **exception → save FAILED then `Err`** (distinct, both tested) via a `try_stage!` wrapper. All 11 progress callbacks reproduced (exact stage/%/current/total — they feed the U-026 SSE). **S-367 `generate_profiles_from_entities` RE-OPENED by DECISION-11 §2:** upgraded sequential → `futures buffer_unordered(parallel_count.max(1))`, making `parallel_profile_count` a **LIVE knob** (was a dead `_parallel_count` — the U-018 gate had deferred concurrency "to the caller"; we ARE the caller). Final ordered Vec + saved file bytes proven **deterministic** across parallel_count ∈ {1,3,10} (indexed-slot writes, `user_id=idx`). opus gate PASS (S-675 + S-367 re-verified); per-entity fallback + realtime-save-non-fatal + bio/persona-distinct preserved. **ALL S-636..S-680 `[x]`/`[≠]` ⇒ U-023 COMPLETE.**
  - **cycle2 (6f865e4): U-007 `zep_paging.py` COMPLETE** (DECISION-12, **map-onto-substrate, NO new production code** — verification + `[≠]` adjudication). The whole module is Zep-Cloud **network** pagination; teri's in-process petgraph has no network/cursor/pages/transient-I/O. **S-054 `fetch_all_nodes` / S-055 `fetch_all_edges` `[x]` map-onto** — subsumed by U-016's parity-verified `KnowledgeGraphEntityReader::get_all_nodes`/`get_all_edges` over `KnowledgeGraph::get_all_entities`(`node_weights().collect()`)/`get_all_edges`(`edge_references()`) which return EVERY node/edge (no limit/filter/pagination — "all" means all). **S-053 `_fetch_page_with_retry` + S-049/051/052 (page_size/retries/delay) `[≠]` inexpressible** (retry only remote-SaaS transient errors; in-memory read has no I/O to retry — consistent w/ U-016 S-216). **S-050 `_MAX_NODES=2000` cap `[≠]` strict-SUPERSET** — opus adversarially checked EVERY consumer (MiroFish graph_builder/zep_tools/zep_entity_reader + teri AgentPool/filter_defined_entities/prepare_simulation/`summarize_entities`) for a ≤2000 dependency and found NONE (LLM context bounded independently by `ENTITIES_PER_TYPE_DISPLAY=20`/`MAX_CONTEXT_LENGTH=50000`); teri returns the full in-memory set = MORE faithful, not a disguised skip. **Unblocks U-017, U-021.**
- **THIS SESSION (2026-06-17, 8th resume) — 3 ITERATE cycles (cycle budget), all opus-gated PASS; began U-023 (3/4 sub-cycles):**
  - **🔑 cycle1 (6d5d96e): U-018 OASIS profile EXPORT layer — a NO-DOWNGRADE CORRECTION** (DECISION-10). The dependency-confirmation for U-023 surfaced that S-367/369/370/371/372/373 were wrongly `[≠]`'d ("OASIS export not needed"/"orchestrator does it") — but U-023 `prepare_simulation` CALLS `generate_profiles_from_entities` + saves `reddit_profiles.json`/`twitter_profiles.csv`, `get_profiles` READS them, and teri's i18n keys already reference them → CONTRACTUAL (the API serves them). Un-`[≠]`'d + ported in new `src/services/oasis_profile_export.rs` (additive — no edit to verified SocialProfile/PersonaGenerator/serializers): `generate_profiles_from_entities` (sequential, reuses `PersonaGenerator::generate_social`, realtime-save via serializers matching MiroFish's inner closure), `save_profiles`, **`save_reddit_json`/`save_twitter_csv` = DEDICATED writers (NOT the serializers) forcing OASIS defaults** (age=30/gender-always/mbti=ISTJ/country=中国/bio[:150]char/karma=1000; reddit key-order byte-identical via preserve_order; twitter CSV header `[user_id,name,username,user_char,description]`, user_id=row-idx), `normalize_gender`, `save_profiles_to_json` alias. S-368 stays `[≠]` (stdout debug print). Opus found CSV terminator CRLF(py) vs LF(rust) — adjudicated NON-CONTRACTUAL (read path universal-newlines normalizes). `csv` crate added.
  - **cycle2 (f58f0fe): U-023 sub-cycle (b) state types** — opus PASS (32/32). `src/services/simulation_manager.rs`: `SimulationStatus` (8 variants created..failed), `PlatformType` (twitter/reddit), `SimulationState` (17 fields + to_dict 17-key + to_simple_dict 9-key, status as lowercase string). **2 ledger-summary corrections** (source authoritative): 8 statuses NOT 4; 2 platforms NOT 3 (no BOTH).
  - **cycle3 (e310c0c): U-023 sub-cycle (c) SimulationManager + FS persistence + create_simulation + 5 getters** — opus PASS (12/12). `SimulationManager{Mutex<HashMap> cache}`, `from_config` (env OASIS_SIMULATION_DATA_DIR, default ./uploads/simulations, mirrors ProjectManager); _save/_load (cache-first, invalid-status→Err, ensure_ascii=False+2-space state.json); create_simulation (`sim_`+12hex); get_simulation/list_simulations (skip hidden+non-dir, project_id filter)/get_profiles (missing-state→Err, missing-file→[])/get_simulation_config (missing→None)/get_run_instructions. S-680 `get_run_instructions` `[≠]`-partial ADJUDICATED legit (its `python run_*_simulation.py`/conda commands reference scripts+env teri lacks — native SimEngine; emitting them = fabrication; expressible paths simulation_dir/config_file ported + substrate_note). **⚠️ CARRY-FORWARD GATE (recorded on U-023 row):** when U-026 API route lands, teri MUST emit NATIVE run-guidance (teri run/SimEngine::run), not just the substrate_note, or the "how to run" contract downgrades.
- **THIS SESSION (2026-06-17, 7th resume) — 1 ITERATE cycle, opus FAIL→fix→PASS → U-016 COMPLETE:**
  - **cycle1 (5f881e1): U-016 COMPLETE — `ZepEntityReader` → `KnowledgeGraph` adapter (S-214..S-222)** (DECISION-9, map-onto-substrate). `src/services/entity_reader.rs`: `KnowledgeGraphEntityReader<'a>{ graph }` with get_all_nodes/get_all_edges/get_node_edges/filter_defined_entities/get_entity_with_context/get_entities_by_type. Additive `KnowledgeGraph::get_entity_by_id` (reads existing private id index, zero blast radius). `[≠]` (each DECISION-9-recorded inexpressible + verified consumer fallback): EntityNode.summary/attributes, related_edge.fact, edge uuid/attributes, related_node.summary (teri's Entity{id,name,kind}/Relation{kind,weight,valid_at} genuinely lack this data — Zep generates it server-side; emitting empty stays within MiroFish's own envelope). S-215 (api_key/Zep client) + S-216 (`_call_with_retry`) `[≠]` — but the except→None/except→[] error CONTRACTS are ported. **GATE CAUGHT a real downgrade:** a self-loop `X→X` was double-counted (petgraph returns it in BOTH directed passes) vs MiroFish's exclusive if/elif (once, outgoing); reachable (add_relation has no self-loop guard). FIXED in the SHARED `get_neighbor_relations` (skip `edge.source()==idx` in the incoming pass) + regression test. **Opus confirmed the old double-count was ALSO a latent U-018 regression → the fix brings U-018 CLOSER to parity** (its `related_edges` come from the same exclusive scan); 104 agent tests pass.
- **THIS SESSION (2026-06-17, 6th resume) — 3 ITERATE cycles (cycle budget), all opus-gated → U-019 COMPLETE:**
  - **cycle1 (1806b43): U-019 sub-cycle (b) + EntityNode DTO** — opus PASS (35/35). `src/services/entity_reader.rs` (NEW): `EntityNode` {uuid,name,labels,summary,attributes,related_edges,related_nodes} + to_dict(7-key) + get_entity_type (first label ∉{Entity,Node}); `FilteredEntities`. A DISTINCT read-DTO, NOT teri's `graph::Entity`. **ZepEntityReader machinery (S-214..S-219) NOT ported** (Zep-read → KnowledgeGraph adapter = DECISION-9, see next-action). Plus the SimulationConfigGenerator<L> LLM foundation + time/event stages in `src/services/simulation_config.rs`: __init__ + class consts + summarize_entities + build_context + call_llm_with_retry (3 attempts, temp 0.7−attempt*0.1, reuses DECISION-7 `chat()` raw-String NOT chat_json, full JSON-salvage chain) + fix_truncated_json + try_fix_config_json (2 regexes) + generate/parse/default time & event config. Prompts byte-verbatim (opus diffed 1207==1207, 562==562); char-based CJK truncation; finish_reason inexpressible (DECISION-7) → salvage-on-parse-failure subsumes Python's length-branch (strict superset → `[≠]`-class, not a downgrade).
  - **cycle2 (350f0c9): U-019 sub-cycle (c) agent-config generation** — opus **FAIL→fix→PASS** (S-450/451/452). generate_agent_configs_batch (async; byte-verbatim prompt; LLM-failure→rule fallback; BATCH defaults DIFFER from dataclass: posts 0.5/comments 1.0/active_hours [9..=22]), generate_agent_config_by_rule (6 exact rule tables), assign_initial_post_agents (ordered alias table, round-robin, influence-max stable tie-break). **GATE CAUGHT a real downgrade:** the alias inner-loop had an UNCONDITIONAL break → a poster_type matching an alias group whose members are all absent fell through to influence-max while Python continues to the next group (person + [alumni 1.0, official 9.0] → Rust gave 200, Python 100). FIXED (removed the unconditional break) + regression test → re-verified PASS.
  - **cycle3 (be77c5a): U-019 sub-cycle (d) generate_config (S-439)** — opus PASS → **U-019 UNIT COMPLETE (73/73 symbols `[x]`, zero `[≠]`)**. The orchestration driver: total_steps=3+ceil(len/15), progress_callback Option<&mut dyn FnMut(i64,i64,&str)>, the 4 ported stages wired in order, twitter/reddit PlatformConfig literals (reddit ≠ defaults), SimulationParameters built field-for-field, reasoning join " | ". No separate save method (serialization = to_dict/to_json from sub-cycle a). **The 991-line/79-symbol SimulationConfigGenerator is fully ported.**
- **THIS SESSION (2026-06-17, 5th resume) — 2 ITERATE cycles, both opus-gated PASS:**
  - **cycle1 (ed6e4bf): U-015 COMPLETE — S-189 build_graph_async + S-192 set_ontology** (DECISION-8). New `src/services/graph_builder.rs`: `build_graph_async(text,ontology,graph_name,chunk params)->task_id` (create_task graph_build + metadata; capture locale; `tokio::spawn(with_locale(...))` driving `build_with_progress`; complete_task w/ teri-native SUPERSET result {graph_name,graph_info,chunks_processed,graph:<serialized>}; fail_task on Err). `build()` byte-IDENTICAL (delegates to build_with_progress→build_with_progress_and_ontology w/ no-op cb; 6 build tests unchanged). **OWNER NO-DOWNGRADE OVERRIDE of DECISION-8 #2:** architect deferred custom RELATION kinds as `[!]` → OVERRIDDEN, ported `RelationKind::Custom(String)` SYMMETRIC w/ `EntityKind::Custom(String)` (MiroFish set_ontology registers BOTH entity+edge types). `set_ontology(&mut self,&Value)` records BOTH sets, wired into extraction prompts+parsers → opus verified NOT inert ({MediaOutlet,COVERS_TOPIC}→Custom; built-ins still map to built-ins). Custom variants additive (externally-tagged serde, no wire regression). 3 `[≠]` legit (create_graph 10%/wait_for_episodes 60-90%/fetch_graph_info 90% = Zep-inexpressible; batch_size non-contractual). i18n progress.* keys pre-existed.
  - **cycle2 (a0ec455): U-019 sub-cycle (a) — config DATA MODEL** (1 of 4 sub-cycles; **U-019 unit STAYS `[ ]`**). New `src/services/simulation_config.rs`: CHINA_TIMEZONE_CONFIG (`china_timezone_config()->Value`), AgentActivityConfig/TimeSimulationConfig/EventConfig/PlatformConfig/SimulationParameters (+to_dict 13-key decl-order, +to_json 2-space ensure_ascii=False). **S-374..S-429 `[x]` (56)**. Byte-identical differential ×3. active_hours default=range(8,23)=[8..22] 15 elems; generated_at reuses python_isoformat_local.
  - **🔑 GLOBAL CARGO CHANGE this session: `serde_json` + `preserve_order` feature.** Opus verified it is a **PARITY GAIN, not a regression**: MiroFish runs Flask 3.x (JSON_SORT_KEYS=False) + has no `sort_keys` anywhere, so the prior `BTreeMap` alphabetical key order was a LATENT DIVERGENCE. preserve_order makes ALL teri JSON (U-010 action_logger, U-011 project, U-002/U-003 /health, U-012 task, U-014 ontology) byte-faithful to Python insertion order. **→ the U-011 `[≠]` "JSON key-order non-contractual" can be RETIRED** (it's now actually-ordered; retire it on next touch).
- **THIS SESSION (2026-06-17, 4th resume) — 1 ITERATE cycle (heavy: architect+porter+verifier):**
  - **cycle1 (95cbe36): U-008-extend (DECISION-7) + U-014 OntologyGenerator** — opus PASS/PASS. **PART 1** added the parameterized chat surface to `LlmClient` — `chat`/`chat_json(&[ChatMessage],&ChatOptions)` + `ChatRole`/`ChatMessage`/`ChatOptions`, impl on all 3 adapters (OpenAI/Anthropic/Gemini), reusing call_api+strip helpers. This is the FAITHFUL full-signature port of S-058/S-059 (MiroFish chat_json takes messages+temp+max_tokens; teri's complete* took only a single prompt). Additive superset, **opus-confirmed NO U-008 regression** (complete*/stream byte-identical, only `#[cfg(test)]` mocks stubbed). **PART 2** U-014 `src/services/ontology.rs` (new `src/services/`): 9/9 symbols (8 `[x]` + S-180 `generate_python_code` `[≠]` — dead code in source + Zep-Cloud Python codegen for absent substrate; behavior maps onto `EntityKind::Custom`/future S-192). generate() passes system+user+temp 0.3+max_tokens 4096; char-based 50000/100-char truncation (Chinese-safe); _validate_and_process all branches differential-identical. `regex` crate added.
- **THIS SESSION (2026-06-17, 3rd resume) — 1 ITERATE cycle, a TRIPLE-completion:**
  - **cycle1 (2fc9d55): U-002 + U-003 axum serve** — opus PASS. New `src/server.rs`: `create_app(state)->Router` (/health + CORS + before/after logging middleware + Accept-Language→locale middleware) + `serve` (validate-first + FLASK_HOST/PORT-superset addr resolution + ctrl_c graceful shutdown). **TRIPLE ROLL-UP flipped 3 pending symbols `[x]` → completed 3 units:** S-005 JSON ensure_ascii (axum raw-UTF-8) + S-003 SECRET_KEY (new `Config.secret_key` on ApiState) → **U-001 COMPLETE**; S-023 main → **U-002 COMPLETE**; S-040 get_locale Accept-Language branch (the middleware) → **U-005 COMPLETE**. **U-003 stays `[~]`**: S-025 /health `[x]` (matched ACTUAL source `{status:ok,service:teri}` — ledger's `{status:healthy}` was wrong); S-024 create_app `[~]` pending the 3 blueprints + register_cleanup. 2 `[≠]` accepted (Windows UTF-8 reconfigure; Flask debug/threaded). tower 0.4→0.5 (axum 0.7 req). `validate_locale` routed through `i18n::is_supported_locale` (single source of truth).
- **THIS SESSION (2026-06-17, 2nd resume) — 3 ITERATE cycles, all committed + opus-gated (each FAIL-caught-then-fixed where noted):**
  - **cycle1 (d2ad17b): U-005 locale/i18n** port-fresh — opus PASS (17-input differential). `src/i18n/mod.rs` + byte-identical embed `src/i18n/locales/{zh,en,languages}.json`. 6/7 symbols `[x]` (t/t_args, get_language_instruction 7-locale, with_locale/LOCALE task-local replacing thread-local, OnceLock {en,zh}, include_str! embed). **S-040 `get_locale` `[~]`**: task-local branch verified; `has_request_context()`→Accept-Language branch pending-U-002/U-003 (axum). **ROLLED UP U-012 → 29/29 COMPLETE** (S-163/S-164 task messages now route through `i18n::t()`).
  - **cycle2 (9dfe29f): U-011 ProjectManager** port-fresh — opus FAIL→fix→PASS, 40/40. `src/models/project.rs`. Gate caught 2 real downgrades: (1) create_project returned a CLONE → stale `updated_at`; fixed to same-object mutation. (2) from_dict strict `Vec<ProjectFile>` silently collapsed a legacy `files` list to `[]`; fixed to `Vec<serde_json::Value>` verbatim. 1 `[≠]` JSON key-order (non-contractual).
  - **cycle3 (4fc2580): U-010 action_logger** port-fresh — opus FAIL→fix→PASS (×2), 28/28. `src/sim/action_logger.rs`. Gate caught: (1) S-083 `log()` narrowed Python `getattr` method-name resolution (critical/fatal/warn/exception→INFO) → fixed full alias map + `LogLevel::Critical`. (2) S-096/S-097 global-singleton test race under parallelism → fixed ENV_LOCK-style mutex + reset. 1 `[≠]` ACCEPTED: `logger.exception()` trailing `sys.exc_info()` traceback line (inexpressible Python ambient state; level dispatch ported; 0 call sites).
- **🔴 FIRST ACTION NEXT SESSION — open U-022 OR U-017 with a FRESH-CONTEXT ARCHITECT CALL to decompose it into sub-cycles** (both large; verify baseline 917 green + reset cycles_this_session=0 first). (Why this session stopped at 2 of budget-3: U-020 completed cleanly, but NO remaining ready unit completes in one more cycle — U-022 is 96 symbols, U-028/029/030 are 27/27/35-symbol subprocess runners all needing decomposition; U-047 is portable but blocked-partial on U-026; U-050 blocked-partial on U-026/U-024. A clean 1-full-unit boundary beats a rushed partial.)
  - **RECOMMENDED: U-022 `simulation_runner.py:SimulationRunner`** (the social-sim CORE, **96 symbols**, deps U-020✅+U-021✅+U-010✅+U-005✅ — ALL met now, the big unblock from this session + last). Class-level state (`_run_states`, `_processes: Dict[str,subprocess.Popen]`, action queues), drives the simulation lifecycle. **NEEDS AN ARCHITECT SUBSTRATE DECISION (à la DECISION-16):** MiroFish spawns the sim as OS SUBPROCESSES (`subprocess.Popen`) per platform; teri's `SimEngine` is IN-PROCESS (`src/sim`, a tokio task w/ broadcast snapshot channel). So `_processes`/Popen → in-process tokio tasks; the action-queue + IPC wire into U-020's `channel()` (just built) + U-021's `GraphMemoryUpdater`; resolves GAP-SOCIAL-WORLDSTATE. Decompose into many sub-cycles (run-state types → lifecycle start/stop → action-queue drain → IPC/interview wiring → stats). Feeds U-049 (register_cleanup, whose stop_all dep is now met), U-026 (the sim API routes), U-045.
  - **ALTERNATIVE: U-017 `zep_tools.py:ZepToolsService`** (1736L, **102 symbols** — the biggest unit; deps U-007✅/U-008✅/U-005✅). 7 DTOs + `search_graph` → map onto `KnowledgeGraph::search`/`get_subgraph` + shimmy `EmbeddingClient`. Multi-sub-cycle. Feeds U-024 ReportAgent + U-027.
  - **Smaller alternatives (subprocess runners → in-process SimEngine, deps met):** U-028 `run_twitter_simulation` (27 sym), U-029 `run_reddit_simulation` (27 sym, mirror of U-028), U-030 `run_parallel_simulation` (35 sym, both platforms via asyncio→tokio).
  - **CARRY-FORWARD (new this session): U-047 `JSONL_TAIL_CONTRACT` (S-1056)** is PORTABLE (teri's U-010 writes `actions.jsonl`; the offset/from_line/last-offset incremental read is expressible — do NOT `[≠]`-skip it) but its READER is realized at **U-026** (the streaming route). When U-026 lands, implement the offset-tailing reader over teri's `actions.jsonl` (or justify mapping onto the in-process `broadcast` snapshot channel) — then U-047 flips `[x]`. Same blocked-on-U-026 pattern as U-050.
  - **U-049** (register_cleanup): its `stop_all` dep (U-021) is satisfied, still waits on U-022. **The HTTP API (U-025/026/027)** completes U-003 + unblocks the 9 Vue reuse-Y units; the path runs through U-022 (sim) + U-017 (ReportAgent) → routes.
  - **RECOMMENDED: U-021 `zep_graph_memory_updater.py`** (554L, **47 symbols**, deps U-007✅/U-005✅ — newly unblocked this session). Real-time graph-memory update from sim agent activities; `AgentActivity` dataclass + `to_episode_text()` generating NL descriptions for **12 action types** (CREATE_POST/LIKE_POST/DISLIKE_POST/REPOST/QUOTE_POST/FOLLOW/COMMENT/…). **KEY REUSE:** the 12 action types map onto teri's ALREADY-PORTED `Action::Social(SocialAction)` taxonomy (S-TAX-001..019, `[x]`); the updater writes onto the in-process `KnowledgeGraph` (map-onto-substrate, DECISION-9 family — Zep `graph.add` episodes → petgraph `add_relation`/`add_entity`). Decompose: (a) `AgentActivity` + `to_episode_text` 12-template (self-contained, maps onto Action::Social) → (b) the updater core (KnowledgeGraph writes) → (c) `ZepGraphMemoryUpdater.start()`/lifecycle (note its thread-spawn is a U-050 site — use `with_locale`). Feeds U-022 (social sim), U-045, U-049.
  - **ALTERNATIVE: U-017 `zep_tools.py:ZepToolsService`** (1736L, **102 symbols** — the biggest remaining unit; deps U-007✅/U-008✅/U-005✅). High-level Zep retrieval tools for ReportAgent (7 DTOs: SearchResult/NodeInfo/EdgeInfo/InsightForgeResult/PanoramaResult/AgentInterview/InterviewResult + `search_graph`). Map-onto teri's `KnowledgeGraph::search`/`get_subgraph` + the shimmy embeddings (`EmbeddingClient`, GAP-OQ3-EMBED resolved). **Definitely needs multi-sub-cycle decomposition** (architect, à la U-019/U-023). Feeds U-024 ReportAgent + U-027.
  - **THEN U-003 completion** is unblocked once U-025/026/027 (HTTP routes) + `register_cleanup` (U-049) land. Service layer is now substantially complete (U-019/U-023 done) — the path to the routes runs through U-017/U-021/U-022 (the ReportAgent + social-sim feeders) then U-025/026/027, then the 9 Vue reuse-Y units.
  - **⚠️ STANDING CARRY-FORWARD GATES (do NOT lose):** (1) at **U-026** `get_run_instructions` MUST emit NATIVE teri run-guidance (teri run / SimEngine::run), not just the static `substrate_note`, or the "how to run" contract downgrades. (2) at **U-026** the route is where `prepare_simulation` gets wrapped in `TaskManager::create_task` + `tokio::spawn(with_locale(...))` returning `task_id` immediately (confirmed this session in `api/simulation.py:360-618` — it is NOT in `prepare_simulation` itself). (3) **U-050** locale-propagation: `build_graph_async`✅ + request-middleware✅ already comply via `with_locale`; the remaining affected sites (prepare→U-026 route, generate_report→U-024, ZepGraphMemoryUpdater.start→U-021) must each use `with_locale` when ported — U-050 flips `[x]` only when all four comply.
  - **⚠️ CARRY-FORWARD GATE (from this session, recorded on U-023 ledger row):** when **U-026** API route lands, `get_run_instructions` MUST emit NATIVE teri run-guidance (teri run / SimEngine::run invocation), NOT just the static `substrate_note` — else the "how to run" contract is downgraded. The opus gate must enforce this at U-026.
  - **REUSE NOTE:** the `generate_profiles_from_entities`/`save_profiles`/`save_reddit_json`/`save_twitter_csv` are in `src/services/oasis_profile_export.rs` (this session); the config-gen + JSON-salvage helpers are in `simulation_config.rs` (U-019); the entity reader is `KnowledgeGraphEntityReader` (U-016). prepare_simulation WIRES these together — do NOT re-port any.
  - **REUSE NOTE:** the DECISION-7 `chat_json(&[ChatMessage],&ChatOptions)` surface is now built — U-019, U-024 ReportAgent, U-018 OasisProfileGenerator all reuse it for system-prompt + temperature LLM calls (don't re-add it).
  - **U-003 completes** once U-025/026/027 (3 blueprints / 59 routes, hostable on the landed `create_app` Router) + register_cleanup (U-023/U-049) land. Service layer (U-019/U-023) is the right focus before routes; 9 Vue units unblock after routes.
- **Units `[x]` (22):** **U-001**, **U-002 serve**, U-004 logging, **U-005 i18n**, U-006 retry, **U-007 zep_paging** (map-onto), **U-008 LLM** (+DECISION-7 chat/chat_json extend), U-009 SeedIngestor, U-010 action_logger, U-011 ProjectManager, U-012 TaskManager, U-013 text_processor, **U-014 OntologyGenerator**, **U-015 graph build** (+`extend_from_text`), **U-016 ZepEntityReader→KnowledgeGraph**, **U-018 Persona+OASIS serializers+export layer**, **U-019 SimulationConfigGenerator**, **U-020 simulation IPC** (COMPLETE — this session, 2 sub-cycles a/b, file-IPC→in-process channel), **U-021 ZepGraphMemoryUpdater+Manager**, **U-023 SimulationManager**, U-048 sim_end. **Partial:** U-003 (S-024 pending blueprints U-025/026/027 + register_cleanup U-049; S-025 `[x]`). Enablers: GAP-1/2/6/U015-1/OQ3-EMBED/ACTION-TAXONOMY ✅. **Newly dependency-ready (U-020+U-021 both done):** U-022 (deps now ALL met), U-028/029/030, U-047 (portable but realized at U-026). Modules: `src/services/{ontology,graph_builder,simulation_config,entity_reader,oasis_profile_export,simulation_manager,graph_memory,simulation_ipc}.rs` (graph_memory + simulation_ipc NEW recently), `src/server.rs`, `src/i18n/`, `src/models/project.rs`, `src/sim/{mod,action_logger}.rs`, `src/embedding.rs`, `src/task.rs`, `src/agent/mod.rs`, `src/graph/mod.rs` (+EntityKind/RelationKind::Custom, +get_entity_by_id, self-loop fix, +extend_from_text/extract_and_merge_into).
- **LLM/embeddings (prior session, still current):** model default `OpenThinker3-7B` + base_url shimmy `127.0.0.1:11435/v1` (Gemma-4-12B doc alt); **GAP-OQ3-EMBED RESOLVED** — shimmy `/v1/embeddings` (FlexNetOS/shimmy#6 MERGED) + teri `EmbeddingClient` (`src/embedding.rs`), embed_model `all-MiniLM-L6-v2`.
- **`[≠]` RE-AUDIT (owner-flagged) — DONE this session** (findings/parity.md `[≠] RE-AUDIT`): re-challenged all 47 `[≠]` under the tightened bar (harness PR #34). 4 disguised skips → ported (above, parity-pending). 2 reclassified to pending deps: **S-189 build_graph_async → U-012** (TaskManager), **S-192 set_ontology → U-014** (OntologyGenerator) — port WITH those units, not `[≠]`. Legit keepers HOLD: Zep-SaaS lifecycle (create_graph/_wait_for_episodes/delete_graph/GraphInfo DTOs), retry jitter, REFRESH (FILTERED_ACTIONS), is_supported json-superset, const-arrays-inline, prompt-folding. **One still-flagged:** S-371 gender 中文-normalization KEEP-`[≠]` (b) BUT must port IF/when OASIS export is consumed (sole caller is OASIS export).
- 9 Vue reuse-Y units BLOCKED on the teri axum API (defer until U-025/026/027 exist).
- **Harness upgrades merged:** harness_hub PR #30, #31, #32, **#33** (reuse-Y is now PROVISIONAL — budget as differential-verify+probable-small-port, not free win).
- **⚠️ develop tip was BROKEN:** `c894de8` (PR #4 merge) was a bad merge (dup `api_key` in config.rs + dead `from_env`/dup block in main.rs) — did NOT compile. **Repaired on `port/mirofish` (commit 9836238)** by restoring the clean fix-branch versions, but **NOT yet PR'd to develop** — develop's tip is still broken until this lands. Consider a clean repair-only hotfix PR (without the Cargo.toml `[workspace]` worktree-aid) to unbreak develop.
- **Branch:** `port/mirofish` (PR #5 → develop, OPEN/held) in worktree `/home/drdave/Desktop/meta/.worktrees/mirofish-port/teri`. **HEAD = 1afbe0c** (c6a5006 handoff → 1afbe0c cargo-fmt-whole-branch). **PUSHED to origin/port/mirofish** (this session — was 38 commits unpushed; now current). develop repaired to `7c354a5` via PR #6. This session (11th resume): 6233ae8 (U-020a IPC protocol types) → 8613d11 (U-020b Client+Server, DECISION-16 → U-020 COMPLETE) → [this handoff checkpoint]. Prior session (10th): 723f5f3 (U-021a) → fa8fd8b (U-021b + extend_from_text, FAIL→fix→PASS) → aa5c2e0 (U-021c → U-021 COMPLETE) → 6549dc6 (handoff). Prior session (9th): a94658a (U-023d → U-023 COMPLETE) → 6f865e4 (U-007 COMPLETE) → a2823c6 (handoff). Prior session (9th): a94658a (U-023d → U-023 COMPLETE + S-367 re-verify) → 6f865e4 (U-007 COMPLETE, map-onto) → a2823c6 (handoff). Prior session (8th): 6d5d96e (U-018 export layer) → f58f0fe (U-023b) → e310c0c (U-023c) → bd48600 (handoff). Prior session (8th): 6d5d96e (U-018 export layer) → f58f0fe (U-023b) → e310c0c (U-023c) → bd48600 (handoff). Prior session (7th): 5f881e1 (U-016 complete, FAIL→fix→PASS) · c81bd8e (handoff). Prior session (6th): 1806b43 (U-019b + EntityNode) · 350f0c9 (U-019c, FAIL→fix→PASS) · be77c5a (U-019d → U-019 complete) · e4a5fa8 (handoff). Earlier (5th): ed6e4bf (U-015 complete) · a0ec455 (U-019a) · 14c5486 (handoff). Earlier: 95cbe36 (U-014 + U-008-extend) · 2fc9d55 (U-002/U-003) · 4fc2580 (U-010) · 9dfe29f (U-011) · d2ad17b (U-005) · ac0ff4e (handoff) · f225c17 (U-012) · ee2c9bd (EmbeddingClient) · c3747cf (LLM model) · d0f4953 (U-001) · 8239b62 ([≠]-audit) · 9836238 (repair) · 064655c (U-015 init) · 4cdfd0d (valid_at+vec_sim). **Cargo.toml dep changes (this branch): tower 0.4→0.5 (axum 0.7) · +regex 1 (U-014) · serde_json +`preserve_order` feature (U-019a — PARITY GAIN, see above).** teri's standalone Cargo.lock is untracked/gitignored in this worktree → no `--locked` risk on this branch; regen the standalone lock at develop→main promote (when the `[workspace]` aid is stripped).
- **Cargo.toml `[workspace]`** = worktree-only build aid (teri is a meta-root member); strip before develop→main promote.
- **Source:** `/home/drdave/Desktop/meta/MiroFish` (Python; real source = backend/app, backend/scripts, backend/run.py, frontend/src, locales — **NOT** backend/.venv).
- **Target==Dest:** teri (port lands Rust directly into teri modules; no separate merge repo).
- **Substrates locked:** ollama→shimmy (done) · Zep Cloud→petgraph+redb · OASIS subprocess→native SimEngine.

## State files (all committed at 897ba5d)
- `parity-ledger.md` — 50 units U-001..U-050 + SWEEP-1..4, all `- [ ]`.
- `symbol-map.md` — 1,087 symbols S-001..S-1087, all `- [ ]` (the symbol-grain DONE gate).
- `target-architecture.md` — per-unit class + 3 Port-and-map blocks + OQ-1..6 resolutions + module layout + idiom map.
- `merge-ledger.md` — class-tagged rows (reuse-Y 18 · port-fresh 17 · map-onto 13 · extend-Y 6).
- `reports/research.md` — X⟷Y reuse map. `reports/cross-repo-refs.md` — teri↔shimmy + intra-teri blast radius.
- `baseline.md` / `findings/y-regression.md` — **teri 142-test baseline = the no-downgrade-of-Y gate**.
- `evaluation.md` / `proposed-upgrades.md` — evolution-find output.

## No-downgrade gate (every ITERATE cycle must hold)
- teri `cargo build` + `cargo clippy -D warnings` + `cargo test` green; **142 baseline tests never regress**.
- Each unit differentially parity-verified vs MiroFish (or, for map-onto, behavioral equivalence of the mapped teri path). Never stub, never drop a branch, never fake a `- [x]`.

## Next ITERATE units
0. **✅ DONE this session:** the `[≠]`-audit parity gate (S-360/361/356/048, all `[x]`) AND **U-001 AppConfig** (partial `[~]`, 20 fields `[x]`). Both committed + verified.
1. **🔴 NEXT (recommended): U-002 `serve_cmd` + U-003 `create_app` (axum server)** — porting these brings SECRET_KEY/JSON_AS_ASCII into a live HTTP surface → **rolls U-001 up to `[x]`** and unblocks U-025/026/027 (HTTP API, 59 routes) + the 9 Vue reuse-Y units. Alternatively the smaller self-contained leaves: **U-012 TaskManager** (deps:none → then port reclassified S-189 build_graph_async) · **U-005 locale** (deps:none, i18n thread-local → feeds U-036).
2. **U-024 ReportAgent ReACT** (the biggest extend-Y) — extend report::generate_stream with plan_outline + a per-section graph-tool ReACT loop using KnowledgeGraph::search/get_subgraph + panorama via valid_at + insight via vec-sim; chat mode; section-by-section progress. NOTE GAP-OQ3-EMBED blocks the embeddings-backed semantic-search path until the substrate decision.
3. **remaining extend-Y / deps**: U-049 graceful shutdown · U-036 i18n · **U-014 OntologyGenerator** (then port S-192 set_ontology, OQ-5/GAP-3 EntityKind::Custom).
4. **port-fresh** (the heavy surface): HTTP/axum API routes (U-025/026/027, 59 routes — unblocks the 9 Vue reuse-Y units), simulation lifecycle manager (U-023), community platform adapters + social-sim consuming the Action taxonomy (U-022/028/029/030, resolves GAP-SOCIAL-WORLDSTATE), IPC/interview (U-020), config generator (U-019), ontology (U-014, OQ-5), Vue re-pointed at teri's axum API (OQ-4).
5. **Vue reuse-Y verify (9 units, BLOCKED until the axum API exists)** — U-035/037/038/039/040/042/043/044 + SWEEP-1: verify the kept-Vue shapes against teri's API once routes land.

## Open gaps (flagged `- [!]`, no silent drop)
- **GAP-SOCIAL-WORLDSTATE** — rich social world-state (timeline/posts/engagement) deferred to U-022/028/029/030.
- ✅ **GAP-OQ3-EMBED RESOLVED 2026-06-17** — owner chose (a): shimmy gained real `/v1/embeddings` (candle BERT all-MiniLM-L6-v2, FlexNetOS/shimmy#6 MERGED) + teri `EmbeddingClient` (`src/embedding.rs`) wired to it; embed_model default → `all-MiniLM-L6-v2`. Unblocks U-017/021/024 vec-search consumers.
- ✅ **GAP-U015-1 RESOLVED** (cycle-5) — `build()` large-doc chunking.

## Loop discipline
Each /loop iteration: run ITERATE cycles (cycle_budget 3, or to ~50% context) → `/harness-evolution find` → commit → `ScheduleWakeup`. DONE only at 100% parity-ledger `- [x]` + 100% symbol-map `- [x]`/`- [≠]` + both left-behind sweeps clean + teri green + Vue re-pointed at teri API (OQ-4). Then open PR `port/mirofish → develop` (auto-merge armed).
