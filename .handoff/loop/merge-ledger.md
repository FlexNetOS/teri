# MiroFish → teri Merge Ledger

**Architect:** rust-port-architect · **Date:** 2026-06-14 · **target==dest==teri** (port and merge collapse).
**Schema:** `- [ ] <unit-id> · <class> · <rust-symbol or —> · <landing> · -> <teri-target> · refs: intra-teri · status`
**Classes:** `port-fresh` | `extend-Y` | `reuse-Y` | `map-onto-substrate`.
`reuse-Y`/`map-onto-substrate` rows are **verify-only vs MiroFish X (skip fresh port)** — confirm teri's existing surface matches X behavior; do not re-implement.
Status legend: `pending` (not started).

---

## Layer 0 — Config / Bootstrap
- [ ] U-001 · extend-Y · `AppConfig` · merge-into `teri::config` · -> teri::config::AppConfig · refs: main.rs, all consumers · pending
- [ ] U-002 · extend-Y · `serve_cmd` · merge-into `teri::main` · -> teri::main::serve_cmd · refs: server::create_app · pending
- [ ] U-003 · port-fresh · `create_app` · new module src/server/mod.rs · -> teri::server::create_app · refs: api::{graph,simulation,report}, /health · pending

## Layer 1 — Utilities
- [ ] U-004 · reuse-Y · — · verify-only `teri` tracing · -> tracing setup (main.rs) · refs: — · pending · verify-only vs X (skip fresh port)
- [ ] U-005 · port-fresh · `i18n::t` · new module src/i18n/mod.rs · -> teri::i18n · refs: report, api, SWEEP-2 · pending
- [ ] U-006 · reuse-Y · — · verify-only `src/llm.rs` · -> OpenAiAdapter retry (llm.rs:35) · refs: — · pending · verify-only vs X (skip fresh port)
- [ ] U-007 · map-onto-substrate · — · map-onto `src/graph` · -> KnowledgeGraph::get_all_entities/edges · refs: graph · pending · verify-only vs X (skip fresh port)
- [ ] U-008 · reuse-Y · — · verify-only `src/llm.rs` · -> OpenAiAdapter::complete/complete_json · refs: GAP-6 <think> add · pending · verify-only vs X (one ADD: <think> strip)
- [ ] U-009 · reuse-Y · — · verify-only `src/seed` · -> SeedIngestor::from_file · refs: — · pending · verify-only vs X (skip fresh port)
- [ ] U-010 · port-fresh · `PlatformActionLogger` · new module src/sim/action_logger.rs · -> teri::sim::action_logger · refs: sim::SimEngine · pending

## Layer 2 — Models
- [ ] U-011 · port-fresh · `ProjectManager` · new module src/models/project.rs · -> teri::models::project · refs: api::graph · pending
- [ ] U-012 · port-fresh · `TaskManager` · new module src/models/task.rs · -> teri::models::task · refs: api::*, sim::manager · pending

## Layer 3 — External Integration Services
- [ ] U-013 · reuse-Y · — · verify-only `src/seed` · -> seed chunk/preprocess · refs: — · pending · verify-only vs X (skip fresh port)
- [ ] U-014 · port-fresh · `OntologyGenerator` · new module src/graph/ontology.rs · -> teri::graph::ontology · refs: graph (EntityKind::Custom), llm · pending
- [x] U-015 · map-onto-substrate · `KnowledgeGraph::build` · map-onto `src/graph` (wire pipeline) · -> teri::graph::KnowledgeGraph::build · refs: extraction helpers, llm, models::task · VERIFIED-IN-TERI 2026-06-14 · build() pipeline landed + parity-verified (5 branches match MiroFish; 156-test baseline intact; findings/parity.md). Open `- [!]` GAP-U015-1: chunking → U-013
- [ ] U-016 · map-onto-substrate · — · map-onto `src/graph` · -> get_entity_with_context/get_entities_by_type · refs: graph::get_subgraph · pending · verify-only vs X (skip fresh port)
- [ ] U-017 · port-fresh · `ZepToolsService` · new module src/services/zep_tools.rs · -> teri::services::zep_tools · refs: GAP-1 valid_at, GAP-2 vec, sim::ipc, report · pending · verify-only vs X (needs valid_at + vec impl)
- [ ] U-018 · extend-Y · `PersonaGenerator` · merge-into `src/agent` · -> teri::agent::Persona (social fields) · refs: agent/mod.rs:14, sim · pending
- [ ] U-019 · port-fresh · `SimConfigGenerator` · new module src/sim/config_generator.rs · -> teri::sim::config_generator · refs: llm, sim::SimConfig · pending
- [ ] U-020 · port-fresh · `InterviewBus` · new module src/sim/ipc.rs · -> teri::sim::ipc · refs: sim::SimEngine::inject_fn · pending
- [ ] U-021 · map-onto-substrate · `KnowledgeGraph::append_episode` · map-onto `src/graph` · -> teri::graph::append_episode · refs: AgentActivity::to_episode_text, sim tick · pending · verify-only vs X (append_episode must land)

## Layer 4-6 — Runner / Manager / Report
- [ ] U-022 · map-onto-substrate · `SimRunner` · map-onto `src/sim::SimEngine` · -> teri::sim::SimEngine · refs: action_logger, ipc, broadcast · pending · verify-only vs X (native — no subprocess)
- [ ] U-023 · port-fresh · `SimulationManager` · new module src/sim/manager.rs · -> teri::sim::manager · refs: models::task, graph, agent, config_generator · pending
- [ ] U-024 · extend-Y (CONFIRMED, not reuse-Y) · `ReportAgent` · merge-into `src/report` · -> teri::report::ReportAgent · refs: report::generate_stream, graph search (U-017), llm · pending · ARCH: findings/u024-architecture.md — BOTH paths coexist (template `PredictionReport` + ReACT `Report`); ZepTools wiring = `ReportTools<'g>{graph:&KnowledgeGraph}` facade (DECISION-9, zero blast radius, leaf struct), NOT Arc; graph_id=[≠] handle/PORTED label. Sub-cycles a→i: (a)data-model✓[parity PASS] (b)ReportTools-wiring✓[BLOCKER CLEARED — parity PASS, is_invalid bug fixed] (b2)insight_forge[OQ-3] (c)tool-dispatch✓[parity FAIL→fix→PASS: gate caught tier-1-normalization + int-coercion downgrades, both fixed] (d)plan_outline✓[parity PASS 7 surfaces; stateful ReportAgent+new_react; FIXED get_graph_statistics HashMap order downgrade] (e)ReACT-loop✓[parity PASS 12 surfaces; full branch ladder verbatim; 7 report_logger sites deferred to (g)] (f)ReportManager✓[parity PASS 7 surfaces, 29 methods byte-identical; regex content-shaping, JSON formats, back-compat; FIXED updated_at→python_isoformat_local] (g1)ReportLogger✓[parity FAIL→fix→PASS — new src/report/logger.rs, 13 helpers + log core (agent_log.jsonl compact, key-order timestamp/elapsed_seconds/report_id/action/stage/section_title/section_index/details, python_isoformat_local ts, 2dp banker's elapsed); wired 7 jsonl markers in generate_section_react (Option<ReportLogger> field, None=no-op); gate caught *_length byte-vs-char downgrade (.len()→.chars().count() ×4, Python len(str)=chars), fixed] (g2)ReportConsoleLogger✓[parity PASS 8 surfaces — architect-decided (findings/u024-g2-console-logger.md): per-report tracing Layer gated on process-global sink (1:1 of Python FileHandler-on-named-loggers); new src/report/console_logger.rs + additive src/logging.rs init; format [%H:%M:%S] LEVEL: msg, WARN→WARNING (#1 trap, correct), INFO+ floor, targets teri::report exact + teri::services::zep_tools prefix; 17 loop emissions wired (unconditional); verifier PROVED real capture (refuted test-skip concern, dumped console_log.txt); fwd-dep [!] zep_tools producers] | (g) COMPLETE (g1+g2). (h)generate_report [ARCH: findings/u024-h-generate-report.md — ReportSink=TRAIT (orthogonal to teri generate_stream, no downgrade); decomposed h1-h4. h1✓[parity PASS — new src/report/sink.rs (ReportEvent{progress:i32}/ReportStage lowercase/ReportSink trait/NullSink); FIXED update_progress u32→i32 parity bug (Python writes -1 on failed path, byte-identical progress.json); +console_logger field on ReportAgent; +ensure_report_folder pub +upload_folder accessor] h2✓[parity FAIL→fix→PASS (5+ surfaces) — generate_report skeleton: report_id uuid12-shape, status machine Pending→Planning→Generating→Completed/Failed, loggers create (log_start/planning_start/planning_complete/report_complete/error), plan_outline via prog//5 sink-closure (RefCell bridge: ProgressCallback is Fn, ReportSink::event is &mut), save_outline, placeholder assemble, both console_logger.close tails. GATE CAUGHT error-tail downgrade: built FRESH failed_report dropping outline/markdown_content/completed_at vs Python except mutating SAME report (retains them in FAILED meta.json) — FIXED by hoisting report before try-body so error arm mutates same object; new test drives EISDIR post-planning failure, asserts on-disk meta.json non-null outline. S-763 stays [~]→h2-skeleton-PASS (full contract completes at h3 section loop)] h3✓[parity PASS round-1 (5 surfaces) — per-section streaming loop replaces h2 placeholder assemble: per-section progress 20+int((i/total)*70), section sink-closure base+int(prog*0.7/total) verbatim, generate_section_react (landed e), section.content=…, push context, save_section IMMEDIATELY (clean_section_content), push completed-title, log_section_full_complete(.trim()==.strip()), per-section update_progress (base / base+70/total), post-loop 95 assembling + REAL assemble over populated files. TRAP#1 CONFIRMED faithful: report.outline re-assigned post-loop (clone-vs-Python-reference) so final meta.json carries section content, intermediate stays empty. TRAP#2 sink superset ADJUDICATED LEGAL: faithful (a)pre-section/(b)closure/(c)95 emits match Python progress_callback + 1 superset content-carrying sink event (NO Python-observable artifact changes, architect §1/§7.5). interview_agents honest-err [!] tolerated. S-763 FLIPPED [x] (section loop = last core piece)] h4[U-027 SSE sink adapter — DEFERRED, lands with U-027 routes]] (i)chat✓[parity PASS — ReportAgent::chat conversational 2-iter ReACT (report_agent.py:1766-1881): CHAT_SYSTEM_PROMPT_TEMPLATE/CHAT_OBSERVATION_SUFFIX/MAX_TOOL_CALLS_PER_CHAT=2 consts verbatim; get_report_by_simulation 15000-CHAR cap + 截断 suffix; 3-slot .format render SHA-256-matched to Python (single JSON-brace unescape); messages system→history[-10:]→user; loop max_iter=2 take(1)/round MAX cap break; (?s) regex cleanup ×2 + .trim(); ChatResponse{response,tool_calls,sources}+to_dict key-order; sources=params.query. ALL 3 truncations CHAR-based (g1-class verified CJK). [≠] fetchReportFailed warning (Option no-error-surface, observable preserved); [!] interview_agents (U-020). S-748/749/753/764 [x]] >>> U-024 FUNCTIONALLY COMPLETE a✓b✓c✓d✓e✓f✓g1✓g2✓h1✓h2✓h3✓i✓ — REMAINING: h4 (U-027 SSE adapter, DEFERRED→lands w/ U-027 route, NullSink covers parity) + b2 insight_forge (OQ-3 query_vec_similarity, PENDING). Unit stays [ ] until h4+b2. interview_agents needs U-020 (d)plan_outline (e)ReACT-loop (f)ReportManager (g)loggers/Sink (h)generate_report (i)chat. Risks: [!]insight_forge/OQ-3, [!]interview_agents/U-020, text-tool-protocol-not-native (PORT verbatim).

## Layer 7 — HTTP API
- [x] U-025 · port-fresh · `graph routes` · new module src/api/graph.rs · -> teri::api::graph · COMPLETE (10/10 routes, all parity-verified in Y) · ARCH: findings/u025-architecture.md — SHARED route seam (U-026/U-027 inherit): DECISION-U025-1 LLM-in-state = construct OpenAiAdapter per-request via build_llm(config) (NOT Arc<dyn LlmClient> — DECISION-14 non-dyn; NOT in ApiState — DECISION-11 caller-constructs; byte-faithful to MiroFish per-request service ctor); DECISION-U025-2 ApiState stays {config} (TaskManager=global() singleton, ProjectManager=per-handler from_config, NO llm field); DECISION-U025-3 graph_router(Arc<ApiState>)->Router + un-stub create_app nest /api/graph CORS-scoped (server.rs:184-193 TODO) + ApiError newtype IntoResponse {success:false,error[,traceback][,extra]} in api/mod.rs (shared); DECISION-U025-4 /build delegates to build_graph_async (U-015) wrapping ported project-state machine (graph_id=task_id, terminal status via task poll). GAPS: [!]U025-GRAPHSTORE (no graph-by-id store — data/delete MAP-ONTO graph_id→task_id→result["graph"]; durable GraphStore deferred to own unit, NOT dropped); [≠]U025-ZEP-TEMPORAL (Zep bitemporal node/edge fields inexpressible, U-015 class); [≠]U025-TRACEBACK (3-key shape kept, value Rust-not-Python); [≠]U025-ZEPGUARD? (keep ZEP_API_KEY guard, gate decides); [!]U025-CLONE (derive Clone on OpenAiAdapter); [!]U025-ROUTE-ORDER (/project/list vs /project/:id); [!]U025-FILEPARSER. Sub-cycles: (a)shared-seam+create_app (b)project read/del/reset 1-4 (c)ontology multipart 5 (d)build 6 (e)task 7-8 (f)data/delete 9-10[gap routes, last]. Crit path a→b→c→d→f; (e)∥. All 17 api.* i18n keys present. PROGRESS: (a)✓+(b)✓ [parity PASS round-1, S-794/795/796/797 [x] — landed together (a-infra not independently testable): src/api/mod.rs ApiError newtype IntoResponse (client 2-key / client_with extra / server 3-key) + build_llm(config)->OpenAiAdapter (per-request, NOT in ApiState) + pub mod graph; src/api/graph.rs graph_router wiring 4 project routes (get_project/list_projects/delete_project/reset_project, Result<Json,ApiError>, ProjectManager::from_config per-handler); server.rs create_app UN-STUBBED (api_router = .nest("/graph",graph_router) under /api, CORS scoped to /api/* — U-026/U-027 add 1 nest line). Flask type=int fallback replicated (?limit=abc→50 not 400). Byte-faithful bodies (preserve_order: get [success,data] / list [success,data,count] / delete [success,message] / reset [success,message,data] / 404 [success,error] / 500 [success,error,traceback]); data==Project::to_dict (15 keys). U025-TRACEBACK [≠] UPHELD legit (3-key shape kept, traceback value Rust backtrace = non-contractual opaque). U025-ROUTE-ORDER [!] RESOLVED (axum 0.7 static-before-capture, real-HTTP-path tested). U025-CLONE deferred to c/d. 16 new tests, 1247 green, clippy clean, 19 server tests not regressed. Atomic gate: X-parity PASS + Y-green + Y-not-regressed. S-024 create_app STAYS partial (flips [x] when all 3 blueprints land).] (c)✓ ontology/generate route 5 [parity FAIL→fix→PASS, S-798 [x]: multipart (axum Multipart + DefaultBodyLimit 50MB) → create_project → per-file allowed_file+save_file_to_project(bytes)+SeedDocument::from_file().raw_text (=FileParser.extract_text)+preprocess_text → all_text header `\n\n=== {orig} ===\n{text}` + total_text_length CHAR count → save_extracted_text → build_llm→OntologyGenerator::generate → project.ontology 2-key projection {entity_types,edge_types}+analysis_summary separate → 6-key response. GATE CAUGHT 2: (1) allowed_file used Path::extension() ≠ Python os.path.splitext on leading-multi-dot (..txt→Python rejects ext='' / Rust accepted) → FIXED dedicated splitext-faithful allowed_file (skip leading dots, rfind, reuse seed::is_allowed_ext canonical set; is_supported UNCHANGED); (2) handler coverage gap (no test drove real handler to 200, phantom inner doc) → FIXED extracted generate_ontology_inner<L> (handler does multipart+validation then calls inner w/ build_llm; new test drives inner w/ MockLlm asserting real 6-key 200 envelope). DERIVED Clone on OpenAiAdapter (U025-CLONE done). U025-FILEPARSER resolved (pdfium from_file + preprocess landed). 1271 green.] (e)✓ task routes 7-8 [parity PASS round-1, S-800/801 [x]: get_task GET /task/:id (TaskManager::global().get_task→Option, None→404 api.taskNotFound, Some→{success,data:to_dict}), list_tasks GET /tasks (global().list_tasks(None)→Vec<Value> ALREADY to_dict'd, no double-serialize, {success,data,count}). Singleton (OnceLock) = Python __new__ singleton, cross-request visibility holds. 4 new tests, 1275 green.] (d)✓ build route 6 [parity PASS round-1, S-799 [x] — THE big one. ARCHITECT REFINED §4a (option ii was a NARROWING the no-downgrade gate forbids — Python build_task sets project.graph_id/status=COMPLETED/FAILED+error from the bg thread, all served by GET /project/<id>): chose (A) ADDITIVE EXTEND. NEW build_graph_async_with_completion(...,Option<ProjectCompletion{manager,project_id}>) — build_graph_async KEEPS 7-arg shape (delegates None, U-015 byte-unchanged, zero blast radius); hook in OUTER build_graph_worker (inner stays project-agnostic): SUCCESS→project.status=GraphCompleted+graph_id=Some(task_id)+save, FAILURE→status=Failed+error+save (best-effort, swallows save errs). ProjectManager +Clone (PathBuf). Handler (graph.py:283-372,515-522, §R.10 17 steps): ZEP guard→500 configError (KEPT, config.zep_api_key exists), project_id→400, lookup→404, status machine (Created→400 ontologyNotGenerated / GraphBuilding&&!force→400+task_id / force-reset), graph_name/chunk falsy-fallback defaults (MiroFish Graph/500/50), get_extracted_text→400 textNotFound, ontology→400 ontologyNotFound, build_graph_async_with_completion (creates task+spawns, SUBSUMES Python create_task), set GraphBuilding+graph_build_task_id+save, 200 {success,data:{project_id,task_id,message}}. Completion hook PROVEN through-disk (reload asserts status/graph_id/error on success+failure). [≠] U025-TASKNAME (graph_build vs 构建图谱, legit display label), U025-GRAPHID-TIMING (set at COMPLETED not spawn, non-observable — frontend reads graph_id post-complete), U025-TRACEBACK carried. Empty body tolerated. 18 new tests, 1289 green.] (f)✓ data/delete routes 9-10 [parity FAIL→fix→PASS, S-802/803 [x]: get_graph_data MAP-ONTO graph_id→task_id→task.result["graph"] (SerializableKnowledgeGraph {entities,edges}) reshape into Python get_graph_data shape {graph_id,nodes[6-key],edges[14-key],node_count,edge_count} — teri-present (uuid/name/labels, name/fact_type/source/target+names via node_map/valid_at/invalid_at) populated, Zep-only [≠] (summary/attributes/created_at/expired_at/episodes/fact) defaulted to Python or-fallbacks; node/edge_count from array lengths. delete_graph ZEP guard + success envelope {success,message:graphDeleted}. GATE CAUGHT edge-uuid downgrade: Uuid::new_v4() PER-REQUEST (nondeterministic, breaks frontend GraphPanel.vue :key + self-loop Set state) → FIXED deterministic Uuid::new_v5(NAMESPACE_OID, "{src}|{tgt}|{kind}|{valid_at}") (stable across calls, survives refetch; verifier cross-computed offline). [≠] U025-ZEP-TEMPORAL (Zep bitemporal fields inexpressible, key shape preserved, edge uuid synthesized-stable), [!] U025-GRAPHSTORE (no durable graph store — delete no-op, response faithful, durable GraphStore deferred to own unit). 10 new tests, 1297 green.] >>> U-025 COMPLETE — all 10 routes parity-verified. Final [!]/[≠]: U025-ZEP-TEMPORAL, U025-GRAPHSTORE, U025-TRACEBACK, U025-TASKNAME, U025-GRAPHID-TIMING (all challenged+survived). create_app S-024 STAYS [~] partial until U-026+U-027 blueprints land.
- [ ] U-026 · port-fresh · `simulation routes` (33 routes) · new module src/api/simulation.rs + nest in server.rs · -> teri::api::simulation · refs: SimulationManager+SimulationRunner<OpenAiAdapter> (in-state Arc), KnowledgeGraphEntityReader, ReportManager::get_report_by_simulation, TaskManager, ProjectManager · ARCHITECTED → findings/u026-architecture.md (DECISION-U026-1: concrete monomorphization Arc<SimulationRunner<OpenAiAdapter>> in ApiState; NO SSE — all poll/JSON; 13 sub-cycles a-m; gaps GAP-U026-SOCIALDB[!] posts/comments/interview-history blocked on U-028/029/030+sqlite-feature, ZEPKEY/SCRIPTDL [≠]) · PROGRESS: (a)✓ [router-only skeleton, NO route logic — gate = DECISION-U026-1 monomorphization compiles in axum State + Y-green + Y-not-regressed (structural; no X route-behavior surface yet, so parity-verifier deferred to b–m). LANDED: src/api/mod.rs ApiState EXTENDED with sim_manager:Arc<SimulationManager> + sim_runner:Arc<SimulationRunner<OpenAiAdapter>> (concrete monomorphization at state boundary — DECISION-U025-1 preserved: no dyn, no generic state); ApiState::new(config) builds BOTH internally from config (sim_manager=from_config, graph_mgr=GraphMemoryManager::<OpenAiAdapter>::new(), runner shares the SAME sim_manager Arc + config.oasis_simulation_data_dir) so all 39 create_app/test call-sites stay 1-arg → ZERO blast radius (the [!] ApiState::new sig-change risk mitigated as architect specified). NEW src/api/simulation.rs (pub mod simulation; in api/mod.rs) simulation_router(Arc<ApiState>)->Router = Router::new().with_state(state) skeleton + full 33-route/13-sub-cycle doc map. server.rs: .nest("/simulation", simulation_router(state.clone())) added to api_router (CORS-scoped /api/*). 1 skeleton test (monomorphization-constructs-through-ApiState::new + router builds). 1298 green (+1), clippy --all-targets clean, Y-not-regressed (zero tests lost). Atomic gate PASS.] (c)✓ create/get/list ×3 [parity FAIL→fix→PASS, S-807/810/811 [x] + S-680 extension. ARCHITECT DECISION-U026-2 (findings/u026-c-run-instructions.md): the U-023 carry-forward gate FIRES here — get_simulation READY branch appends run_instructions=get_run_instructions(id), which MUST emit NATIVE run-guidance not just substrate_note. EXTEND (not [≠]): RunInstructions += commands:RunCommands{twitter,reddit,parallel} + instructions:String + to_dict() (key order simulation_dir,config_file,commands{...},instructions,substrate_note; scripts_dir DROPPED = the only [≠]). LANDED src/api/simulation.rs: POST /create (create_simulation: empty-body tolerant, project_id 400 requireProjectId, project 404 projectNotFound, graph_id=body.graph_id||project.graph_id 400 graphNotBuilt, enable_twitter/reddit default-true, USES state.sim_manager in-state Arc per DECISION-U026-1 NOT fresh manager — faithful: same FS-backed state.json + cross-request cache; {success,data:state.to_dict()}), GET /:simulation_id (get_simulation: 404 simulationNotFound, result=state.to_dict(), READY gate inserts run_instructions), GET /list (list_simulations: ?project_id filter only — source-confirmed NO ?limit at simulation.py:797; {success,data:[to_dict],count}). Router: /create,/list(static),/:simulation_id(capture) static-before-capture axum 0.7. GATE CAUGHT real downgrade: native commands referenced POST /api/simulation/{id}/start (id-in-PATH) but authoritative start route is POST /start with simulation_id+platform in BODY (simulation.py:1451-1505, S-820 — NO /<id>/start route exists) → guidance unroutable when (g) lands → FIXED to POST /api/simulation/start body:{simulation_id,platform} (+ synced doc-comments) + added route-SHAPE regression asserts in BOTH tests (get_run_instructions_structural_fields + get_simulation_ready_has_run_instructions) so revert-to-path-form fails. Re-verified PASS. i18n keys (requireProjectId/projectNotFound/graphNotBuilt/simulationNotFound) byte-identical en/zh, {id} interpolation matches. +13 tests (1296→1309), clippy --all-targets clean, Y-not-regressed. Atomic gate PASS.] (b)✓ entities ×3 [parity PASS round-1, S-804/805/806 [x]. LANDED src/api/simulation.rs 3 read-only handlers + private load_entity_reader_graph helper (graph_id→task→result[\"graph\"]→KnowledgeGraph::deserialize_from_json→KnowledgeGraphEntityReader::new(&g) — reuses U-025(f)/DECISION-9 graph-load-by-id convention; ZEP guard KEPT matching source+U-025 get_graph_data, U026-ZEPKEY [≠] resolved by KEEPING). GET /entities/:graph_id (filter_defined_entities(entity_types csv,enrich)→FilteredEntities::to_dict {entities,entity_types,total_count,filtered_count}); GET /entities/:graph_id/:entity_uuid (get_entity_with_context→Option, None→404 entityNotFound {id}-interp via t_args, Some→EntityNode::to_dict); GET /entities/:graph_id/by-type/:entity_type (get_entities_by_type→Vec, data {entity_type,count,entities} order, count==len). enrich parse = case-insensitive ==\"true\" (NOT generic bool — ?enrich=1→FALSE, ?enrich=TRUE→TRUE; verifier ADDED these guards, the highest divergence risk the porter hadn't tested). Routing: /entities/:g/:u (3-seg) vs /entities/:g/by-type/:t (4-seg) axum segment-count disambiguation, non-vacuous response-shape test. VERIFIER FLAGGED defensible [≠] U026-R2-ABSENTGRAPH: Route-2 unknown-graph_id→500 (teri can't construct reader over nonexistent graph) vs Python blanket-except→404; PRIMARY contract (valid graph+missing entity→404) ported faithful, only secondary absent-graph status differs, both {success:false,error}, consistent w/ U-025(f) precedent — substrate-forced input-domain narrowing, NOT a dropped feature. i18n entityNotFound/zepApiKeyMissing en/zh present. +18 tests (1309→1327, incl 4 verifier guards), clippy --all-targets clean, Y-not-regressed. Atomic gate PASS.] (e)✓ profiles/config reads ×5 [parity FAIL→fix→PASS round-2, S-813/814/815/816/817 [x]. LANDED src/api/simulation.rs 5 handlers: GET /:id/profiles (sim_manager.get_profiles(id,platform default reddit)→{platform,count,profiles}; TeriError::Sim→404, else→500), GET /:id/profiles/realtime (DIRECT file read reddit_profiles.json|twitter_profiles.csv; sim_dir missing→404 simulationNotFound{id}; read errors→[]+warn; state.json is_generating=status==preparing, total_expected=entities_count raw 0-not-null; key order {simulation_id,platform,count,total_expected,is_generating,file_exists,file_modified_at,profiles}), GET /:id/config/realtime (DIRECT read simulation_config.json; generation_stage 4-branch machine generating_config/generating_profiles/completed/null; key order {simulation_id,file_exists,file_modified_at,is_generating,generation_stage,config_generated,config[,summary]}; summary 8-key only when config truthy-object), GET /:id/config (sim_manager.get_simulation_config→None 404 configNotFound), GET /:id/config/download (Response bytes + Content-Type application/json + Content-Disposition attachment filename simulation_config.json; missing→404 configFileNotFound). NEW helper python_isoformat_local_from(SystemTime) in models/project.rs (= datetime.fromtimestamp(st_mtime).isoformat() local naive, micros-omit-when-zero) for [≠] U026-MTIME (file_modified_at; absent→null). get_simulation_dir→pub(crate). GATE CAUGHT 2 real downgrades (no-downgrade gate working): (1) S-814 CSV csv::Reader default flexible(false) hard-errored ragged rows → dropped ALL rows incl valid (Python csv.DictReader is LENIENT — this realtime route polls a file MID-WRITE) → FIXED ReaderBuilder flexible(true)+DictReader padding (short row→trailing null, long row→surplus array under "null" key, restkey=None→jsonify "null"); (2) S-815 summary gate config.is_object() TRUE for {} but Python `if {}:` FALSE → FIXED config.as_object().is_some_and(|o|!o.is_empty()). Round-2 verifier built standalone csv-crate-vs-DictReader differential harness — row-for-row agreement on ALL edges (blank-line skip, exact-count no-padding, quoted-comma, whitespace, surplus serialization); summary truthy non-dict→Python AttributeError→500 (non-contractual, under existing [≠] U025-TRACEBACK). NO new [≠]/[!] — CSV is full parity. +17 tests (1327→1346 incl 4 round-2 regression), clippy --all-targets clean, Y-not-regressed. Atomic gate PASS.] (f)✓ generate-profiles ×1 [parity PASS round-1, S-819 [x]. LANDED src/api/simulation.rs handler generate_profiles (POST /generate-profiles): empty-body tolerant (body:Option<Json<Value>>), graph_id absent/empty→400 requireGraphId, entity_types optional / use_llm default true / platform default reddit, load_entity_reader_graph (ZEP guard kept, U026-ZEPKEY [≠]) → KnowledgeGraphEntityReader::new(&g).filter_defined_entities(types,enrich=true), filtered_count==0→400 noMatchingEntities, generate via NEW oasis_profile_export::generate_profiles_no_cb (PersonaGenerator::new + build_llm OpenAiAdapter + Some(&graph) enrichment + parallel_count=5 Python-default + realtime_output None + no-op cb), format via NEW pub(crate) format_profiles_for_platform(pairs,platform) reusing build_persona_wrapper→Persona::{to_reddit_format|to_twitter_format|to_dict} match (reddit/twitter/else-to_dict), 200 {success,data:{platform,entity_types,count,profiles}} key order, count==len. STRUCTURAL: generate_profiles_no_cb is a SYNC wrapper (tokio block_in_place + Handle::block_on + no-op Send cb) — the generator holds &mut dyn FnMut (!Send) across await so the future is !Send but axum handlers require Send; the wrapper isolates the !Send future so it never crosses the handler boundary. Verifier ADJUDICATED SAFE: teri serve = bare #[tokio::main] (multi-thread default, no current_thread flavor) so block_in_place can't panic; Flask is synchronous so request-blocks-until-done is faithful (NOT a downgrade); simulation_manager.rs:1339 caller UNCHANGED (wrapper purely additive). filter_map(None) faithful no-op (build_persona_wrapper always Some(social) → format methods never None → 1:1 with Python list-comp, no count divergence). [~] U026-f-ENTITY_TYPES_ORDER (entity_types sorted for determinism; both sides unordered sets → set-equality contract, defensible, set contents preserved). +19 tests (1334→1353, 7 handler + 12 unblocked by compile fix), clippy --all-targets clean, Y-not-regressed. Atomic gate PASS.] (g)✓ start/stop ×2 [ARCHITECTED findings/u026-g-architecture.md (RunInputs verdict: BLOCKED→honest-degradation). g1 /stop parity PASS FULL (S-821 [x]); g2 /start boundary-parity PASS (S-820 [~]). LANDED src/api/simulation.rs: POST /stop (stop_simulation→get_simulation→status=Paused→save_simulation_state→200 run_state.to_dict; None-state still 200; map_runner_err TeriError::Sim→400 else→500 = Python ValueError→400/Exception→500 faithful), POST /start (FULL boundary: id 400 requireSimulationId, platform default parallel / ∉{twitter,reddit,parallel} 400 invalidPlatform, coerce_max_rounds=Python int() [JSON int ok, float TRUNCATE-toward-zero int(5.7)=5 then ≤0→maxRoundsPositive, numeric-string "5"→5, "5.7"/"abc"→maxRoundsInvalid strict-int, bool→invalid non-contractual], get_simulation None 404 simulationNotFound, status≠Ready state-machine: check_simulation_prepared→is_prepared, Running+live runner_status=="running"→force?stop(swallow):400 simRunningForceHint, force→cleanup_simulation_logs+force_restarted, →Ready+save / not-prepared→400 simNotReady, graph_id resolution state.graph_id||project.graph_id else 400 graphIdRequiredForMemory). NEW pub(crate) check_simulation_prepared helper (sim_dir+4 required files, prepared_statuses=[ready,preparing,running,completed,stopped,failed]&&config_generated, **preparing→ready AUTO-UPGRADE state.json write** updated_at=python_isoformat_local indent-2 — observable, tested via disk re-read; write-failure-ordering matches Python exactly [returned status stays "preparing" on write-err]; CJK reason strings byte-exact; info-dict key order preserved). NEW SimulationRunner::cleanup_simulation_logs + CleanupResult (deletes exactly Python 9-item set run_state.json/*.log×3/{twitter,reddit}_simulation.db/env_status.json/{twitter,reddit}/actions.jsonl — NOT config/profiles; absent→skip; runs.remove(id)=Python del cls._run_states[id] faithful). save_simulation_state→pub(crate). THE GAP (architect-sanctioned, NO fabrication — no MockLlm/fake run_state/stub): at the start_simulation/RunInputs call point an honest ApiError::server 500 carries marker GAP-U026-RUNINPUTS-BUILDER — teri has NO production RunInputs{engine,pool} builder (engine needs SimConfig::from_simulation_config, pool needs profile→AgentPool reader; both produced by U-028/029/030, all unported). Verifier PROVED a fully-valid prepared request reaches exactly this 500 (whole boundary executes). +24 tests (1353→1377), clippy --all-targets clean, Y-not-regressed. Atomic gate PASS (X-boundary-parity + Y-green + Y-not-regressed).] (e2)✓ script-download ×1 [parity PASS, S-818 [≠] U026-SCRIPTDL (owner-decision 404-vs-drop → KEEP-route-with-404). LANDED src/api/simulation.rs: handler download_script + const ALLOWED_SCRIPTS (4 entries verbatim run_{twitter,reddit,parallel}_simulation.py + action_logger.py) + route /script/:script_name/download (static seg0 → axum ranks above /:simulation_id/config/* capture, no collision). FULL validation boundary ported VERBATIM: script_name ∉ allowed → 400 unknownScript with name + `allowed` rendered as Python str(list) repr `['run_twitter_simulation.py', ...]` (single-quote items/", " sep/brackets — matches t() value.replace). [≠] divergence (architect findings/u026-architecture.md:103/165/182, cartographer S-601): teri ships NO run_*.py (native in-process, DECISION-17 — same reason scripts_dir dropped from get_run_instructions().to_dict() in (c)); valid name → 404 scriptFileNotFound (the SAME 404 MiroFish returns when os.path.exists is False, same {success:false,error} body + name interp). Owner-decision honored: route KEPT (never silently dropped), never 200-empty (architect's explicit prohibition). VERIFIER PASS (challenge-survived): refuted lazy-drop — confirmed no scripts/ dir + no run_*.py producer anywhere in repo, reconstructed exact Python str(list) and asserted allowed_repr equality, 4xx branches carry no traceback key. +3 tests (1377→1380), clippy --all-targets clean, Y-not-regressed. Atomic gate PASS (X-faithful-surface + [≠]-adjudicated + Y-green + Y-not-regressed).] (h)✓ run-status/detail ×2 [parity FAIL→fix→PASS, S-822/823 [x]. LANDED src/api/simulation.rs: run_status (GET /:id/run-status — get_run_state None→200 8-key idle stub {simulation_id,runner_status:idle,current_round:0,total_rounds:0,progress_percent:0[INT],twitter/reddit/total_actions_count:0}, NOT 404, NOT full to_dict; Some→200 run_state.to_dict()), run_status_detail (GET /:id/run-status/detail — None→200 5-key idle stub {simulation_id,runner_status:idle,all_actions:[],twitter_actions:[],reddit_actions:[]}; Some→to_dict + 5 keys in Python mutation order all_actions/twitter_actions/reddit_actions/rounds_count/recent_actions, reads U-047 actions.jsonl tail via get_all_actions, ?platform filter w/ Python falsy gating). +2 routes (router). GATE CAUGHT real downgrade in SHARED primitive (no-downgrade gate, U-022(d) verification had missed this edge — (h) is the first consumer exposing ?platform=): empty-string ?platform= is FALSY in Python (`not platform` True → get_all_actions("") reads BOTH files + `if platform_filter and …` skips record filter) but teri treated Some("") as a literal platform → read NEITHER file → all_actions/recent_actions wrongly []. FIXED in src/services/simulation_runner.rs: get_all_actions `no_filter = platform.is_none()||platform==Some("")` gates both reads; read_actions_from_file record filter `if let Some(pf)=platform_filter.filter(|p|!p.is_empty())`. Verifier-confirmed Python source (simulation_runner.py:918/929 outer, :869 inner). RE-VERIFIED PASS: empty ?platform= now all=2/recent=2/tw=1/rd=1; NO over-correction (Some("twitter") still filters all=1/rd=0); NO None-path change; NO shared-primitive regression (get_actions/get_timeline/get_agent_stats callers pass None, none inject Some("")). +6 tests (1377→1386 incl regression run_status_detail_empty_platform_query_reads_both). `[!] U026-h-ACTIONS-PRODUCER-PENDING` (action lists empty until U-028/029/030 wire SimEngine→actions.jsonl; read path PROVEN with real jsonl). `[~] U026-h-ROUNDS-LIVE` (rounds_count 0 on file-loaded state both sides, faithful for the route's read path). clippy --all-targets clean, Y-not-regressed. Atomic gate PASS.] (i)✓ actions/timeline/agent-stats ×3 [parity FAIL→fix→PASS, S-824/825/826 [x] (+ primitives S-619/620/621/622 verified-through-consumer, correcting a U-022(d) gap). LANDED src/api/simulation.rs 3 handlers: get_simulation_actions (GET /:id/actions — Flask type=int limit/offset default 100/0 + agent_id/round_num None-default, get_actions, {count,actions:[AgentAction.to_dict]}), get_simulation_timeline (GET /:id/timeline — start_round/end_round, get_timeline, {rounds_count,timeline:[TimelineEntry.to_value]}), get_simulation_agent_stats (GET /:id/agent-stats — get_agent_stats, {agents_count,stats:[AgentStats.to_value]}). +3 routes. GATE CAUGHT 3 real downgrades in SHARED U-022(d) primitives (the (i) routes are the first CONSUMER exposing TimelineEntry/AgentStats serialization — S-621/622 were NEVER actually verified): (1) TimelineEntry DROPPED first_action_time/last_action_time (Python 9 keys, teri 7 — fields never tracked, bare TODO) → FIXED added fields to TimelineRound+TimelineEntry + get_timeline tracks per Python DESC-iteration (first-seen=NEWEST, last-overwrite=OLDEST, names intentionally inverted) + to_value appends after action_types; (2) AgentStats::to_value key order — action_types emitted LAST but Python puts it BEFORE first/last timestamps (preserve_order → byte-observable) → FIXED reordered; (3) latent get_agent_stats last_action_time NEVER updated in loop (stuck at first-seen) → FIXED per-iter assignment. RE-VERIFIED PASS: full 9-key/8-key order asserted + DESC timestamp values; no over-correction (/actions + AgentAction.to_dict untouched; to_value is the only wire path, derive(Serialize) unused on routes); no regression (1376 lib + 1391 all-targets green, no stale U-022(d) test asserted old shape). Flask type=int fallback (?limit=abc→100 not 400) verified. `[!] U026-i-PRODUCER-PENDING` (action lists empty until U-028/029/030 wire SimEngine→actions.jsonl; read+group+stats paths PROVEN via real-jsonl fixtures). `[~]` active_agents set-order non-contractual; `[~]`/[≠] negative ?limit (usize-parse→default, U-025 precedent, non-contractual). clippy --all-targets clean, Y-not-regressed. Atomic gate PASS.] (j)✓ posts/comments ×2 [parity PASS round-1, S-827/828 [x]. LANDED src/api/simulation.rs: get_simulation_posts (GET /:id/posts — platform default reddit, Flask type=int limit/offset 50/0; missing-DB→200 4-key {platform,count:0,posts:[],message:dbNotExist}; populated→{platform,total,count,posts}), get_simulation_comments (GET /:id/comments — reddit_simulation.db, optional post_id; missing-DB→200 2-key {count:0,comments:[]}; populated→{count,comments}). +2 routes + helper social_db_path (no-create join under oasis_simulation_data_dir/<id>). ARCHITECT directive (findings:123-128): port route shapes + missing-DB empty branch NOW (faithful current behavior — no producer → no DB → always empty), defer populated SELECT. LANDING: empty branch fully ported+tested in DEFAULT build; populated SELECT * FROM post/comment ORDER BY created_at DESC LIMIT/OFFSET + COUNT(*) IMPLEMENTED behind #[cfg(feature=sqlite)] (rusqlite, mirroring get_interview_history_from_db; sqlite_row_to_object row→dict by column name; OperationalError→empty fallback; post_id filter) — verifiable NOW via hand-built test DB, ready for producer; #[cfg(not(sqlite))]+DB-exists → HONEST 500 GAP-U026-SOCIALDB (no fabrication, NO silent empty). VERIFIER PASS round-1 (traced all 3 Python sim-dir paths [read-route uploads/simulations, OASIS_SIMULATION_DATA_DIR config, producer RUN_STATE_DIR] resolve to SAME physical dir → teri's oasis_simulation_data_dir/<id> faithful, same dir cleanup deletes *_simulation.db; honest-500 adjudicated correct no-downgrade landing vs silent-empty; OperationalError→empty + post_id filter + COUNT-unpaginated all confirmed). ALSO fixed pre-existing --all-features clippy warning in get_interview_history_from_db (if-let-Ok→flatten, behavior-identical) keeping sqlite path green. `[!] GAP-U026-SOCIALDB` OPEN (producer+feature frontier, same as GAP-SOCIAL-WORLDSTATE). +5 tests (default +3: 2 empty + 1 honest-500; sqlite +2 populated), 1394 default green / 1395 sqlite green, clippy --all-targets AND --all-features clean, Y-not-regressed. Atomic gate PASS.] (k)✓ interview ×4 + helper [parity PASS round-1, S-829/830/831/832 + optimize_interview_prompt helper [x] (5 symbols, 50 test executions). LANDED src/api/simulation.rs: 4 POST routes (body-driven, no path params) /interview /interview/batch /interview/all /interview/history + helpers optimize_interview_prompt (CJK prefix INTERVIEW_PROMPT_PREFIX byte-identical hex-verified; empty→empty, no-double-prefix), parse_timeout, shape_interview_result (IPCResponse→Python dict: {success, agent_id+prompt | interviews_count, result|error, timestamp} key order, CommandStatus::Completed=="completed"), validate_interview_platform, count_interview_agents. +4 routes. ARCHITECT directive: routes 1-3 IPC-gated (check_env_alive true only w/ running IPC server from U-028/029/030 producer). FULL validation + env-alive gate ported+TESTED (requireSimulationId/requireAgentId[agent_id 0 VALID]/requirePrompt/invalidInterviewPlatform/requireInterviews/per-item interviewListMissing*+InvalidPlatform 1-based index; env Err→false→400 envNotRunning faithful to Python check_env_alive False). The IPC success path (optimize→send→shape) ported but env-gated [!] IPC-PRODUCER-PENDING (every valid request 400s envNotRunning today — the faithful current contract). Primitives return raw IPCResponse (U-022(e) split), so route does the shaping; /all interviews_count reconstructed via count_interview_agents (IDENTICAL agent_id-present predicate to runner's built-list). history route sqlite-gated (no-DB→200 empty both builds; #[cfg(sqlite)] reads trace action='interview'; no-sqlite+DB-exists→honest-500 GAP-U026-SOCIALDB). VERIFIER PASS (byte-verified CJK prefix; agent_id 0 falls through to requirePrompt; env-gate 400-not-500; shape key-order by inspection vs Python primitive tails; interviews_count predicate-identical; history all 3 branches). Fixed 2 collapsible-if clippy (let-chains) + (prior j) pre-existing interview-history clippy. RESIDUALS CARRIED: `[!] U026-k-IPC-PRODUCER-PENDING` (success path runtime-unverifiable until U-028/029/030, code-inspected); `[≠] U026-k-TIMEOUT504` (teri TeriError has NO Timeout variant → IPC timeout maps Sim→400 vs Python TimeoutError→504; MUST-RESOLVE when producer lands — add TeriError::Timeout→504, i18n keys interviewTimeout/batchInterviewTimeout/globalInterviewTimeout already staged; acceptable now ONLY because the timeout path is unreachable [same producer-pending umbrella]); `[~] U026-k-AGENTID-TYPE` (non-integer agent_id → teri 400 vs Python passes, defensive narrowing on malformed input, unreachable today). +30 tests (default 1409 / sqlite 1410 green), clippy --all-targets+--all-features clean, Y-not-regressed. Atomic gate PASS.] · NEXT: l env-status/close-env ×2 (env-status get_env_status_detail PURE FILE read — portable TODAY; close-env close_simulation_env IPC → [!] IPC-PRODUCER-PENDING), m history ×1; d prepare(+status) ×2 folds in w/ lifecycle group. U-026 progress: a✓c✓b✓e✓f✓ g(g1✓ g2[~]-producer-gap) e2✓[≠] h✓ i✓ j✓ k✓ of a-m · in-progress
- [ ] U-027 · port-fresh · `report routes` · new module src/api/report.rs · -> teri::api::report · refs: report::ReportAgent, graph search · pending

## Layer 8 — Simulation Platforms (native; was OASIS subprocess)
- [ ] U-028 · map-onto-substrate · `TwitterPlatform` · map-onto `src/sim::SimEngine` · -> teri::sim::platform (Twitter) · refs: Action enum, action_logger · pending · verify-only vs X (reimplement OASIS twitter natively)
- [ ] U-029 · map-onto-substrate · `RedditPlatform` · map-onto `src/sim::SimEngine` · -> teri::sim::platform (Reddit) · refs: Action enum, action_logger · pending · verify-only vs X (reimplement OASIS reddit natively)
- [ ] U-030 · map-onto-substrate · `MultiPlatformRunner` · map-onto `src/sim` top-runner · -> teri::sim::multi_runner · refs: two SimEngine (tokio::join), OQ-1 · pending · verify-only vs X (dual-platform native)

## Layer 9 — Frontend (Vue kept, re-pointed — OQ-4)
- [ ] U-031 · port-fresh · `axios client` · keep-Vue frontend/src/api/index.js · -> teri axum API baseURL · refs: i18n Accept-Language · pending
- [ ] U-032 · port-fresh · `graph client` · keep-Vue · -> teri /api/graph · refs: U-025 routes · pending
- [ ] U-033 · port-fresh · `simulation client` · keep-Vue · -> teri /api/simulation · refs: U-026 routes · pending
- [ ] U-034 · port-fresh · `report client` · keep-Vue · -> teri /api/report · refs: U-027 routes · pending
- [ ] U-035 · reuse-Y · — · keep-Vue (unchanged) · -> Vue reactive store · refs: — · pending · verify-only vs X (skip fresh port)
- [ ] U-036 · extend-Y · — · keep-Vue + `src/i18n` · -> vue-i18n + locales/*.json · refs: SWEEP-2 · pending
- [ ] U-037 · reuse-Y · — · keep-Vue (unchanged) · -> vue-router · refs: — · pending · verify-only vs X (skip fresh port)
- [ ] U-038 · reuse-Y · — · keep-Vue (unchanged) · -> Home.vue · refs: U-032 · pending · verify-only vs X (skip fresh port)
- [ ] U-039 · reuse-Y · — · keep-Vue · -> MainView.vue · refs: U-032/U-033 · pending · verify-only vs X (verify task-poll shapes)
- [ ] U-040 · reuse-Y · — · keep-Vue · -> SimulationView.vue · refs: U-033 · pending · verify-only vs X (verify config/profile shapes)
- [ ] U-041 · extend-Y · — · keep-Vue · -> SimulationRunView.vue · refs: U-033/U-034, SSE · pending
- [ ] U-042 · reuse-Y · — · keep-Vue · -> ReportView.vue · refs: U-034 · pending · verify-only vs X (verify agent-log from_line)
- [ ] U-043 · reuse-Y · — · keep-Vue · -> InteractionView.vue · refs: U-034 · pending · verify-only vs X (verify chat shape)
- [ ] U-044 · reuse-Y · — · keep-Vue (unchanged) · -> main.js+App.vue · refs: U-036/U-037 · pending · verify-only vs X (skip fresh port)

## Layer 10 — Runtime / Concurrency contracts
- [ ] U-045 · map-onto-substrate · — · map-onto tokio runtime · -> tokio tasks · refs: sim, graph · pending · verify-only vs X (daemon threads → tokio tasks)
- [ ] U-046 · map-onto-substrate · `CancellationToken` · map-onto SimEngine · -> tokio_util CancellationToken · refs: sim::SimEngine · pending · verify-only vs X (no subprocess)
- [ ] U-047 · map-onto-substrate · — · map-onto channels · -> InterviewBus (U-020) · refs: sim::ipc · pending · verify-only vs X (file-IPC → channels)
- [ ] U-048 · reuse-Y · — · verify-only `src/sim` broadcast · -> SimEngine::subscribe_with_history · refs: — · pending · verify-only vs X (skip fresh port)
- [ ] U-049 · extend-Y · `shutdown handler` · merge-into `src/main`+`src/server` · -> graceful shutdown · refs: sim CancellationToken · pending
- [ ] U-050 · port-fresh · `LOCALE task-local` · new src/i18n (task-local) · -> teri::i18n::LOCALE · refs: U-005, spawn sites · pending

## Sweep items
- [ ] SWEEP-1 · reuse-Y · — · keep-Vue · -> 6 step components · refs: re-pointed clients · pending · verify-only vs X (skip fresh port)
- [ ] SWEEP-2 · port-fresh · `locales` · src/i18n/locales + keep-Vue · -> zh/en keys · refs: U-005, U-036 · pending
- [ ] SWEEP-3 · map-onto-substrate · `SimEngine` · map-onto `src/sim::SimEngine` · -> native SimEngine · refs: U-022/U-028/U-029/U-030 · pending · verify-only vs X (RECLASSIFIED: reimplement OASIS natively, was "Python subprocess")
- [ ] SWEEP-4 · — · — · (drop, non-prod) · -> — · refs: — · `- [≠]` intentional-divergence: utility test script, not production surface

---

## Cross-cutting gap rows (no silent drop — each blocks specific units)
- [!] GAP-1 · `Relation.valid_at: Option<(u64,Option<u64>)>` · blocks U-017/U-021 panorama_search active/historical · OQ-2
- [!] GAP-2 · `query_vec_similarity` impl via shimmy embeddings · blocks U-017 insight_forge semantic search · OQ-3
- [!] GAP-3 · `EntityKind::Custom(String)` dynamic kinds · blocks U-014 ontology generator · OQ-5
- [!] GAP-4 · per-platform action-validity matrix · blocks U-028/U-029 · Decision-2
- [!] GAP-5 · action-arg enrichment (post/user/comment) · informs U-030 · Decision-2
- [!] GAP-6 · `<think>...</think>` strip in OpenAiAdapter · blocks U-008 parity · Decision-3 → **RESOLVED cycle-4** (strip_think + strip_json_fence, all 3 adapters)

---

## Cycle-4 verification reclassifications (AUTHORITATIVE over the reuse-Y rows above)
The differential gate (reuse is never trusted) refuted the reuse-Y class for every backend unit checked — findings in `findings/reuse-verify-{llm,seed,infra}.md`:
- [x] U-006 · reuse-Y→**extend-Y DONE** · retry recovery+cap+no-spurious tested, MAX_BACKOFF_SECS clamp; jitter `- [≠]`.
- [x] U-008 · reuse-Y→**extend-Y DONE** · `strip_think`+`strip_json_fence` across all 3 adapters; teri LLM = proven superset; GAP-6 resolved.
- [~] U-008 · **extend-Y (pending, DECISION-7)** · additive parameterized `chat`/`chat_json(&[ChatMessage], &ChatOptions)` on all 3 adapters (system+user roles, opt temperature/max_tokens) — `complete`/`complete_json`/`stream` byte-identical (no regression). Built once; reused by U-014 (trigger), U-019, U-024, U-018. Port before U-014. ← `chat_json` callers in `ontology_generator.py:217`, `llm_client.py:35-102`.
- [~] U-009 · reuse-Y→**extend-Y (pending)** · add encoding-fallback (GBK/Latin-1) + `.md` dispatch + `is_supported` gate + multi-file concat. ← file_parser.py.
- [~] U-013 · reuse-Y→**port-fresh (pending)** · teri has NO text chunking → new `src/seed/text_processor.rs` (`split_text` 500/50 + `preprocess` + `stats`). **Unblocks GAP-U015-1.** ← text_processor.py.
- [≠] U-004 · reuse-Y→**intentional-divergence** · teri logs to stdout by design (operators redirect; MiroFish itself does at simulation_runner.py:427); rotating-file appender omitted (owner-optional `tracing-appender` add).
- [~] U-048 (JSONL action-log streaming) · reuse-Y→**extend-Y (pending)** · 3/4 behaviors confirmed (ordering, no-loss/catch-up, backpressure); add in-band end-of-sim terminal signal to stream subscribers (`StreamEvent::sim_end` at sim/mod.rs:496 + api/mod.rs:82). ← action_logger.py:105.
